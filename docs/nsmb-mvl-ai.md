# NSMB Mario vs Luigi AI

## Current Goal - 2026-06-07

1人用MvLのCPU相手を、ルールベースAIだけで終わらせず、次の混合方式で育てる。

1. ルールベースAIで最低限ゲームを成立させる。
2. 人間プレイとルールベースAIのプレイログを集める。
3. 入力ラベル付きの状態ログから模倣学習する。
4. 模倣学習済みAIを初期値にして自己対戦で強化する。
5. 実プレイ向けに反応遅延、ミス率、探索制限で強さ調整する。

## Stage Scope

当面のAI実装・ログ検証・RuleAI調整は、stage 0（草原ステージ）だけを対象にする。tileProbeのoffset、solidish mask、穴/壁判定、ブロック/アイテム箱状態の検証もまずstage 0に固定し、他ステージ固有の地形やギミックは後回しにする。

## State Logging Requirements

最初の必須条件は、人間が画面を見て判断できる状態に近い情報をメモリから取得できること。学習前に、少なくとも次をフレーム単位で保存する。

- 入力ラベル: player別 held/pressed keys、console別 held/pressed keys、touch。
- プレイヤー: actor found、座標、前フレーム座標、速度、状態/アクション/物理/衝突/環境フラグ、死亡/遷移、powerup raw値、reserve item raw値、画面上の変身/ストック候補としての `visualState`、所持/表示スター、Battle Stars、コイン、スコア。
- 目標物: 画面上の Big Star actor、コイン/アイテム、8コイン由来アイテム。`objectId=0x010C settings=0x00001120` は内部markerで、学習用の可視Big Starとしては使わない。
- ワールド: stage、vs mode、local player、カメラ矩形/ターゲット/位置、総コイン数。
- オブジェクト: active objectの object ID、settings、GUID/base、state、flags、lifecycle、skip flags、座標、速度。既知IDはカテゴリ名も付ける。
- 学習補助: frame、instance、role、inGameplay、hash、object counts。

## Implementation Status

- 完了: ルールベースAI本体を `src/frontend/qt_sdl/NsmbRuleAI.cpp` / `.h` に分離した。
- 完了: AI play log v1を監査用として残しつつ、学習用の `nsmb_mvl_compact_observation_v2` を追加した。`scripts/nsmb_mvl_ai_convert_playlog_v2.py` は v1 JSONL / `.jsonl.gz` / `recording.json` / `recordings-index.json` を読み、1フレームを `scalarFeaturesByPlayer`、player別 `terrain`、typed `entities`、`labels` に分けたcompact JSONLへ変換する。通常v2ではfireball debug words、object vtable、raw pointer類を落とし、地形は `33x17` の `sparse_channel_mask_v2` として `solid/coin/question/breakable/brick/entrance/harmful/hiddenOrRescue` などのbit channelで保存する。
- 完了: `scripts/nsmb_mvl_ai_build_compact_dataset.py` を追加し、compact observation v2から `scalar`、`terrain`、`opponent_terrain`、固定上限 `entities`、`labels`、`actions` を分離した `.npz` datasetを作れるようにした。v2 datasetは学習正本として扱い、従来の巨大固定列CSVは互換/baseline用として残す。
- 完了: 学習用地形scalarから曖昧な `tile_wallLeft/right` を外し、`tile_blockedAhead/Left/Right` と `tile_effectiveHoleAhead/Left/Right` を使う形へ整理した。地形の正本は `33x17` sparse terrain gridで、scalarは軽量モデル向けの補助特徴量として扱う。2026-06-14以降、`blocked*` / `effectiveHole*` は17点probeではなくterrain gridから足元床rowを推定して導く。`contact.wallLeft/right` は実ゲームの接触フラグとして残すが、学習・RuleAIの移動阻害判定はgrid由来の `Blocked*` とcontact壁だけを見る。
- 完了: Phase 4のラベル整理として、compact v2の教師ラベルを `up/down/left/right/y/b` の6物理ボタンへ制限し、同時に `horizontal(neutral/left/right)`、`vertical(neutral/up/down)`、`jump(none/press/hold)`、`run(off/on)`、`fire(off/press)` のfactor化行動ラベルを保存する。Yは物理出力ではrun/fire共通のまま、学習ラベルではrun intentとFire状態時のfire tap intentを分ける。Y holdは走行意図として扱い、fireはYが新規に押されたframeだけ `press` にする。旧12ボタンのraw heldは監査用に `held` として残し、学習用には `allowedHeld` と `actions` を使う。
- 完了: v1からcompact v2へ後変換する場合、`appliedPlayer*` には `held` はあるが `pressed` がないため、`pressed` は `player*` / `console*` の値へfallbackするようにした。これにより、Fire状態でYを押したframeが `fire=press` として残る。修正前にv1から作ったv2/datasetはfire labelが全てoffになるため、v1から再変換して使う。
- 完了: `fire(off/press)` へ変えた後のaction labelは `nsmb_mvl_action_labels_v2` とし、古いv2ログの `fireId=2` / `hold_or_repeat` は後方互換で読み替えない。学習dataset化する場合はv1から新v2へ再変換するか、直接v2を取り直す。
- 完了: compact模倣AIの実行時入力では、`fire=press` が出た時に前フレームもY heldなら、そのフレームだけYを離して次フレームにYを押し直す。これにより、ダッシュ中のfire intentでもYのpressed edgeを作る。`MELONDS_NSML_IMITATION_AI_TRACE=1` では `fireTap=release/press/none` を出す。
- 完了: `scripts/nsmb_mvl_ai_train_compact_imitation.py` と `scripts/nsmb_mvl_ai_predict_compact_imitation.py` を追加した。新しい模倣baselineは `scalar + terrain + opponent_terrain + entities` を固定順にflattenしてmulti-head softmaxで各action headを学習し、予測CSVではactionから実際の6ボタンheldへ戻してフレーム単位で確認できる。
- 完了: `scripts/nsmb_mvl_ai_train_torch_imitation.py` を追加し、compact datasetを `scalar MLP + terrain CNN + entity MLP pooling` のPyTorch multi-head policyとしてGPU学習できるようにした。RTX 4070 SUPER / PyTorch 2.5.1+cu121で `logs\codex-imitation-current-human-firefix-20260614-211822\compact-dataset-player1.npz` 102,034行を学習し、基礎操作向けにはクラス重みなしの `logs\codex-torch-imitation-current-human-noweight-20260614-224653\torch-compact-policy.pt` を採用候補にした。best epoch 31、validation exact 0.874、head accuracyは horizontal 0.939 / vertical 0.996 / jump 0.965 / run 0.965 / fire 0.993。fire pressはvalidation実例143件に対して未学習なので、Fire行動は今後の追加ログで増やして再学習する。
- 完了: GPU学習したPyTorch compact policyをC++実行時へ戻す `scripts/nsmb_mvl_ai_export_torch_compact_runtime_model.py` と、Python/C++一致検証用 `scripts/nsmb_mvl_ai_make_torch_compact_runtime_fixture.py --synthetic` を追加した。新runtime schemaは `nsmb_mvl_torch_compact_policy_runtime_v1` で、`scalar MLP + terrain CNN + entity MLP pooling + fusion MLP + multi-head` をmelonDS側の軽量C++ forwardで実行する。`logs\codex-torch-runtime-export-20260614-234010\torch-compact-policy.runtime.json` は元の `.pt` からexportしたruntimeで、`nsmb_mvl_compact_cpp_predict` の64 synthetic fixtureでは Python/C++ prediction exact 1.0 を確認した。
- 完了: compact multi-headモデルをC++ runtime形式へ出す `scripts/nsmb_mvl_ai_export_compact_runtime_model.py`、Python基準fixtureを出す `scripts/nsmb_mvl_ai_make_compact_runtime_fixture.py`、C++推論確認用 `nsmb_mvl_compact_cpp_predict` を追加した。`logs/codex-phase4-smoke-20260614` の64フレームfixtureではC++予測がPython予測と完全一致した。
- 完了: melonDS実行時の `MELONDS_NSML_IMITATION_AI_MODEL` が `nsmb_mvl_compact_action_policy_v1` の場合、旧linear modelではなくcompact multi-head modelとして読み、`scalar + terrain + opponent_terrain + entities` をC++側で直接組んでaction head推論し、6ボタンheldへ戻す。`logs/codex-compact-runtime-melonds-smoke-20260614` では1000F smokeでhost/clientとも `modelType=compact`、`features=17313`、`held=0x862` などのtraceを確認し、closed-loop evalはpassした。2026-06-14の grid-derived `blocked*` 変更後は `logs/codex-terrain-grid-derived-check-20260614` の小モデルで `features=17313` のPython/C++ fixture 64行一致を確認した。
- 完了: melonDS実行時の `MELONDS_NSML_IMITATION_AI_MODEL` が `nsmb_mvl_torch_compact_policy_runtime_v1` の場合、torch compact policyとして読み、既存のcompact runtime feature builderから `18435` featuresを組んでCNN/MLP推論し、6ボタンheldへ戻す。`logs\codex-torch-runtime-closed-loop-20260614-235838` では `modelType=torchCompact`、client側 nonzero input ratio 0.610、host側 0.439、死亡なし、Big Starへの接近あり、trace上の `held=0x820` などを確認した。1200Fの短時間確認であり、実戦品質評価は別途長めのclosed-loopで行う。
- 完了: torch compact policyのC++実行時推論は毎frame CPU forwardだと重く、`logs\codex-torch-runtime-perf-probe-20260615-000729` では active fps が約7.5fpsまで落ちた。実行時に `MELONDS_NSML_IMITATION_AI_INFER_INTERVAL` を追加し、既定を16Fごとの推論 + cached held再利用にした。`MELONDS_NSML_IMITATION_AI_NEUTRAL_HOLD_FRAMES` では短時間のneutral出力を直前非ゼロ入力で吸収できる。`logs\codex-torch-runtime-default-i16-20260615-001944` では同じruntime model、traceなし、AIPlayLogInterval=120で active fps 約44.4fps、frame limit到達を確認した。neutral入力が長く出る状態はfallbackではなく現行モデル品質の課題なので、追加人間ログと再学習で改善する。
- 方針: 将来の対戦AIは60fps維持を必須条件にする。60fpsを割る高精度モデルはmelonDS実行時の本命にはせず、teacher/分析用として扱う。実行時モデルは `feature build + inference + input apply` のp95を1-2ms程度に収める小型student modelを別枠で設計する。今のtorch compact runtimeは観測形式・学習方針・Python/C++一致検証のための検証用であり、そのまま強くするより、計測を入れて `state read / feature build / model forward / input apply / logging` を分解し、allocation削除、float32固定配列、BatchNorm fold、inactive entity skip、地形encoder縮小、必要なら蒸留/量子化を進める。
- 完了: ImitationAI用のneutral/stuck監査 `scripts/nsmb_mvl_ai_audit_imitation.py` を追加し、`run-nsmb-mvl-ai-closed-loop-eval.ps1 -Policy imitation` では host/client の `imitation-audit.json` を自動生成するようにした。`logs\codex-imitation-audit-torch-i16-20260615-010110` ではtorch compact runtimeが client側 neutral比率0.158、neutralWindow 1、stuckWindow 11で、主因は `opponent_near` と判定された。`logs\codex-imitation-audit-compact-linear-20260615-010206` の軽量compact action policyは neutral比率0.040、neutralWindow 0、stuckWindow 2まで下がったが、初期接触付近でYだけ押して位置が動かないstuckが残る。
- 検証: ログ/traceを切った速度上限では、neutral baseline `logs\codex-neutral-speed-nolog-20260615-010402` が active fps 約60.0、軽量compact action policy `logs\codex-compact-linear-speed-nolog-20260615-010317` が active fps 約58.8-58.9。2窓評価ハーネス上限に近く、線形compact系は60fps runtime候補として扱える。一方でtorch compactはteacher/分析用に寄せ、実行時は軽量compactまたはそこから蒸留した小型studentを本命にする。
- 完了: ImitationAI trace時に入力がfallbackした理由を `fallback=playerFilter/notGameplay/compactFeatures/...` として出すようにした。`logs\codex-watch-imitation-trace-debug-20260614-232457` では `compact-imitation-model-current-human-firefix-20epoch.runtime.json` をmelonDS閉ループで実行し、client側 player1 の nonzero input ratio 0.958、coin change 2、trace上の `model=compact held=0x810/0x823` などを確認した。
- 完了: compact v2の同等性検証用に、`scripts/nsmb_mvl_ai_convert_playlog_v2.py --include-legacy-features` と `scripts/nsmb_mvl_ai_legacy_csv_from_v2.py` を追加した。通常v2には巨大な旧CSV行を入れないが、検証時だけ `legacyFeatureRows` を埋め込み、v2から旧CSVを復元して既存 `nsmb_mvl_ai_build_dataset.py` の出力と比較できる。
- 完了: melonDS側で `MELONDS_NSML_AI_OBSERVATION_V2_LOG=<path>` を指定すると、v1からの後変換ではなく実行時に直接 `nsmb_mvl_compact_observation_v2` JSONLを出力できるようにした。v1 `MELONDS_NSML_AI_PLAY_LOG` は監査用として併用可能で、通常v2にはfireball debug words/object vtable/raw pointer類を入れない。未検出playerのterrainは空セルとして出し、検出後は `33x17` sparse channel maskだけを保存する。直接v2出力のlabelsも6ボタン/actions形式に揃えている。
- 完了: `scripts/run-nsmb-mvl-human-recording.ps1` は通常録画でclient側 `client/ai-observations-v2.jsonl` を直接出し、そのv2から `ai-compact-dataset-player1.npz` を生成する。既存の v1 `ai-playlog.jsonl(.gz)`、監査JSON、従来CSVは監査/互換用としてそのまま残す。`scripts/compress-nsmb-mvl-ai-playlogs.ps1` はv1とv2の両方を `.jsonl.gz` へ圧縮し、`recording-session.json` の `clientAIObservationV2Log` / `hostAIObservationV2Log` も更新する。通常録画ではmelonDSを閉じるとpostCommandsが自動実行され、manifest/dataset/audit/gzipまで進む。従来どおり後で手動実行したい場合は `-NoAutoPostprocess` を付ける。
- 完了: 人間録画のpostCommandsから、通常時の巨大な従来CSV `ai-dataset-player1.csv` 生成とCSV terrain auditを外した。2026-06-14の `star-chase` 手動ログでは旧CSVだけで約544MiBになり、3番目のpostCommandが主な遅延要因だった。通常の学習正本は `ai-compact-dataset-player1.npz` なので、旧CSVが必要な互換/調査時だけ `run-nsmb-mvl-human-recording.ps1 -BuildLegacyDataset` を付ける。`run-nsmb-mvl-recording-postcommands.ps1` は各postCommandの所要時間を表示する。
- 完了: 手動録画は人間がmelonDSを閉じて終了するため、`run-nsmb-mvl-human-recording.ps1` から `run-nsmb-mvl-manual-local.ps1` / `run-nsmb-mvl-lan-route-smoke.ps1` へ `SkipFrameLimitCheck` を渡し、自動テスト用の `frame limit reached` ログを要求しないようにした。これにより、正常に閉じた手動録画で `manual local wrapper failed ... missing client frame limit` にならず、そのままpostCommandsへ進める。
- 完了: Windows PowerShellの `Start-Process -PassThru` で、子wrapperが正常終了しても `Process.ExitCode` が空として見えるケースを確認した。`run-nsmb-mvl-manual-local.ps1` は終了コードが空の場合、wrapper stdoutの `NSMB Mario vs Luigi LAN route smoke passed` 成功マーカーと空stderrを確認して成功扱いにする。失敗ログがある場合や成功マーカーがない場合は、従来どおりログパス付きで失敗させる。`logs\nsmb-mvl-human-recording-stage0-20260614-054510` はこの誤判定で止まったが、後処理を再実行して `recording-audit.json` pass、`fireball-audit.json` owner low confidence 0、v1/v2 gzip生成まで確認済み。
- 完了: player actor、Big Star actor/candidate、Battle Starsを使ってremote CPU入力を生成できる。
- 完了: 左右ラップ幅 `MELONDS_NSML_RULE_AI_WRAP_WIDTH=0x400000` を考慮して目標へ向かう。
- 完了: RuleAIの左右ラップ方向計算で、player Xが負座標になったときに `u32` 座標として差分計算され、スター方向が左のままなのに右入力へ反転する問題を修正した。座標はまず符号付き32bitへ戻し、wrap幅で剰余正規化してから左右判定する。`logs/watch-ruleai/client/ai-playlog.jsonl` の終盤スタックでは旧計算だと x<0 で `dx` が正方向へ化けていたが、修正後の同区間は全sampleで左方向のままになる。
- 完了: 同じラップ端問題が模倣学習の特徴量にも影響するため、dataset作成とC++逐次推論の `target_dx`、nearest object/item/fireball dx、AI play logのobject relative/visualSummary dxをwrap込みの最短水平差へ揃えた。旧RuleAIログはfeatureを作り直せばdxは補正されるが、旧RuleAIが出した右入力ラベル自体は残るため、ラップ端を含む旧RuleAIログは再収集または学習対象から除外する。
- 完了: `MELONDS_NSML_AI_PLAY_LOG=<path>` で `JSONL` のAIプレイログを出せるようにした。既存の巨大CSV game-state traceとは別に、学習入力として読みやすい1行1フレーム形式にする。
- 完了: `scripts/nsmb_mvl_ai_build_dataset.py` でJSONLから模倣学習用の固定列CSVを生成できるようにした。
- 完了: `scripts/nsmb_mvl_ai_train_imitation.py` で固定列CSVからnumpyのみの多ラベル模倣学習モデルを学習し、`.npz` に保存できるようにした。
- 完了: AI play logに `visualSummary` を追加し、カテゴリ別active object数、カメラX範囲内のobject数、player別の最近傍カテゴリ距離、objectごとのplayer相対座標とscreen Xを保存するようにした。
- 完了: `scripts/nsmb_mvl_ai_inspect_playlog.py` でJSONLを人間が読むための短い表へ変換できるようにした。
- 完了: `scripts/nsmb_mvl_ai_catalog_objects.py` でJSONL内のactive objectを object ID/settings/category ごとに集計できるようにした。
- 完了: playerの `collisionFlag` / `environmentFlag` を名前付き `contact` 状態へ展開し、接地、予測接地、天井、左右壁、水/液体/水没、流砂、ロープ/ポール、スパイク、コンベア、雪/砂/破壊地形、左右ラップをAI play logとCSV特徴量に保存するようにした。
- 完了: playerごとの `screen.camera0/1` と `fallRisk` をAI play logへ追加した。画面X/Y、カメラ内判定、カメラ底までの距離、下端近接、カメラ下抜け、Y速度符号を保存し、穴/落下判断の前段特徴としてCSVへ展開する。
- 完了: `screen.camera0/1` に `inViewY` を追加し、player/opponent/objectの画面内判定をX/Y/完全判定でログに出せるようにした。
- 完了: `scripts/nsmb_mvl_ai_render_playlog_svg.py` でJSONLの1フレームをplayer中心のSVGに描画できるようにした。表だけでなく、星、hazard、item、coin、敵、platform、unknown objectの相対配置を目視できる。
- 完了: SVGレンダラで `players[].tileProbe.samples` を小さな四角として描画するようにした。タイルサンプルの位置、tile id、behavior、solidish/coin/block/harmful分類をSVG上で目視確認できる。
- 完了: AI play logの `objects[]` に `offset` と `vtable` を追加した。object ID/settingsだけで意味が分からないactorも、vtableを手がかりに後から分類できる。
- 完了: stage 0手動ログ `logs\nsmb-mvl-human-recording-stage0-20260613-191418` のframe 1590/2255を調査し、`objectId=0x01F settings=0x00090002` は箱から出てP1 coin `1 -> 2` へ増える取得コインだったため `coin_item` に分類するよう修正した。同ログのframe 2255に出る `objectId=0x010C settings=0x00001120` は本物の `big_star_actor` とは別の内部markerなので `big_star_marker` とし、GUI/SVGでは非表示、datasetの最近傍Big Star/targetは `big_star_actor` だけを見る。追加で `logs\nsmb-mvl-human-recording-stage0-20260613-203615\client\ai-playlog.jsonl` のframe 1348を確認し、落としたスターは `objectId=0x022 settings=0x00001112` のスターactor variantだったため、`0x022` かつ settings低位が `0x1002/0x1012/0x1102/0x1112` のものを `dropped_star_item` として分類する。
- 完了: `scripts/nsmb_mvl_ai_build_dataset.py` に `--label-source auto|applied|player|console` を追加した。ルールAIログは `appliedPlayerN`、人間プレイログはメモリ上の `playerN` / `consoleN` 入力を教師ラベルにできる。
- 完了: `scripts/nsmb_mvl_ai_predict_imitation.py` で学習済み `.npz` とdataset CSVからオフライン推論し、予測held入力、button別確率、ラベルとの一致率をCSV出力できるようにした。
- 完了: `scripts/nsmb_mvl_ai_export_runtime_model.py` と `src/frontend/qt_sdl/NsmbImitationAI.cpp` / `.h`、`tools/nsmb_mvl_imitation_cpp_predict.cpp` を追加した。現在の `.npz` 線形模倣モデルをC++ランタイム用JSONへexportし、C++側で `x=(features-mean)/scale`、`sigmoid(x @ weights + bias)`、threshold判定を実行できる。将来の自己対戦モデルでは、このruntime model形式をMLPなどへ拡張する。
- 完了: melonDS実行中に `MELONDS_NSML_IMITATION_AI=1` / `MELONDS_NSML_IMITATION_AI_MODEL=<runtime-model.json>` でC++模倣モデルを読み、`GameStateSample`、object scan、tileProbe、fireball handlerからruntime feature vectorを組み立てて、player別入力へ戻せるようにした。PacketBridge JIT scratchのplayer別入力ループにも差し込んだため、`MELONDS_NSML_IMITATION_AI_PLAYER=remote` でもremote player入力として反映できる。現行モデルではfeature coverage `1727/1727` を確認済み。
- 完了: `scripts/nsmb_mvl_ai_evaluate_closed_loop.py`、`scripts/run-nsmb-mvl-ai-closed-loop-eval.ps1`、`scripts/nsmb_mvl_ai_compare_closed_loop.py` を追加した。neutral / RuleAI / ImitationAIを同じstage 0 seedで実走し、AI play logまたはgame-state CSVから survival、death、Big Star距離、star/coin変化、item/projectile/fireball/hazard/block可視frame、入力分布、最終star差、heuristic score、死亡/スター取得/coin/powerup変化frameを `closed-loop-eval.json` に保存し、複数reportを比較できる。
- 完了: `scripts/run-nsmb-mvl-ai-closed-loop-batch.ps1` を追加した。複数policyと複数seedの閉ループ評価を連続実行し、各runの `closed-loop-eval.json`、全体の `closed-loop-batch.json`、比較用 `closed-loop-compare.json/csv` を生成できる。`-Policies neutral,rule,imitation -Seeds "0x...,0x..."` のようにカンマ区切り指定も扱う。
- 完了: ImitationAIの実行時入力に、`moving_hazard` / `hazard` / `enemy_goomba` / `enemy_koopa` をobject scan全体から見て、相対距離、相対速度、接近中/至近判定で最も危険な対象を選ぶhazard guardを追加した。`MELONDS_NSML_IMITATION_AI_HAZARD_GUARD=0` または `MELONDS_NSML_IMITATION_AI_DISABLE_HAZARD_GUARD=1` で無効化でき、横/縦/近距離閾値は `MELONDS_NSML_IMITATION_AI_HAZARD_GUARD_HORIZONTAL_RANGE` / `VERTICAL_RANGE` / `CLOSE_RANGE` で調整できる。これは学習データ生成には影響せず、逐次推論時だけ危険方向入力を抑える実験的な安全フィルタ。
- 完了: RuleAIの `PlayerFrameState` にplayer別hazard threatを追加し、単一の最新moving hazardではなくobject scan全体から選んだ危険対象を見るようにした。逃げ方向の詰まり判定はterrain grid由来の `BlockedLeft/Right` と `contact.wallLeft/right` を使い、近距離hazard時は逃げ方向または水平中立+jumpへ切り替える。`MELONDS_NSML_RULE_AI_HAZARD_HORIZONTAL_RANGE` / `MELONDS_NSML_RULE_AI_HAZARD_VERTICAL_RANGE` で閾値を調整できる。
- 完了: player actor内の `CollisionMgr` を読み、AI play logの `players[].collisionMgr` に collision result、ground collision、modifier tile、attached tile、raw state byteを保存するようにした。逆アセンブルで `CollisionMgr +0x7C` がcollision result、`+0x98/+0x9C/+0xA0` がbottom/top/side modifier tile typeであることを確認し、以前の暫定 `bottomTileType` 読み取りは廃止した。
- 完了: player本体の `+0xBB2/+0xBB3` を `players[].tileDamage` として保存するようにした。`Player::applyTileDamage` / `Player::updateCollision` の逆アセンブルで参照を確認した。
- 完了: `StageLayout::getTileBehavior` / `getChunkID` / `readTileBehaviour` を逆アセンブルし、AI play logの `players[].tileProbe` にプレイヤー周辺/前方/左右17点のタイルサンプルと `33x17` gridを保存するようにした。各点は actor座標からStageLayout pixel座標へ変換し、chunk id、tile id、tile behavior、solid/harmful/coin/block系カテゴリ、`solidish` を出す。summaryにはraw probe由来の `ahead/left/right*Solid` を監査用に残すが、学習/RuleAI用の `groundBelowSolid`、`blockedAhead/Left/Right`、`effectiveHoleAhead/Left/Right` はterrain grid由来にした。v1監査ログには古い `wall*` 名も残るが、学習正本のv2 scalarでは使わない。
- 完了: `StageLayout::readTileBehaviour` の動的tile behavior table参照先を逆アセンブルで再確認し、`tileId >= 0x100` の読み元を `0x020CAD28` へ修正した。旧参照先 `0x0208AF3C` ではstage 0の地面系 `tileId=0x110-0x113` が `status=6` になり、スクショ上の床がgridで未解決セルになっていた。
- 完了: RuleAIの内部 `FrameState` に `GroundBelowSolid` / `BlockedAhead` / `HoleAhead` / `BlockedLeft` / `HoleLeft` / `BlockedRight` / `HoleRight` を追加し、terrain gridから作る派生 `blocked/hole` 判定を使って横移動入力とジャンプ入力を決めるようにした。grid内で足元の床rowを先に推定し、その床rowから身体高さと前方床を導くため、階段床や接地中の近傍solidを左右壁として拾いにくい。`logs/codex-ruleai-terrain-grid-20260614` の3 seed x host/client閉ループはpassし、平均死亡 `0.6667`、平均生存率 `0.9098` を確認した。
- 完了: RuleAIの危険回避で敵カテゴリIDを内部 `FrameState` に渡し、Goomba/Koopa接近時は逃げ方向が詰まっていれば敵方向への踏みジャンプも選べるようにした。ジャンプ出力は学習ラベルと同じBボタンへ揃え、hazard中にBを押しっぱなしにして `pressed` edgeが出ない問題を避けるため、地上の緊急hazard jumpでは1フレームrelease後にB pressを作るジャンプパルスを入れた。hazardの左右またはカテゴリが変わった場合は、古い逃げ方向メモリを破棄する。`logs/codex-ruleai-hazard-pulse-side-reset-20260614` の3 seed x host/client閉ループはpassし、平均死亡 `0.1667`、平均生存率 `0.9484`、平均スター取得 `0.3333` を確認した。残る1死亡は `seed=0x2f52869f` のhost側remote P1表示で、client側P1は同seedで死亡なし・スター取得ありのため、AI判断だけでなくhost/client状態差の影響も疑う。
- 検証: 人間ログ収集の既定相手であるMario側RuleAI向けに、Mario側だけ常時穴復帰を有効にする試作は `logs/codex-ruleai-mario-only-recovery-20260614` でMario側平均死亡 `0.3333`、Luigi側回帰 `logs/codex-ruleai-luigi-mario-gated-regression-20260614` で平均死亡 `0.1667` と数値上は有効だった。ただしP0/P1で判断ロジックを分ける設計は不適切なため、採用方針を撤回し、RuleAIはMario/Luigiで同じ判断ロジックに揃える。対称最小形 `logs/codex-ruleai-mario-symmetric-baseline-20260614` / `logs/codex-ruleai-luigi-symmetric-baseline-20260614` は両側とも平均死亡 `0.6667` で、まだ人間ログ収集相手として不十分。全hazard範囲拡大、敵早期検出、近距離敵をclosingなしで危険扱い、空中blocked抑制、空中guardBrake抑制はいずれも別の落下/接触死を増やしたため不採用。
- 完了: RuleAIをMario/Luigiで同一ロジックに保ったまま、穴中/穴下のBig Starを追わない安全フィルタを追加した。SVG/PNGで `seed=0xdeadbeef` の死亡直前を確認し、P1が縦穴内のスターを取ると壁キックなしでは復帰できないことを確認したため、近くて下方向に深いBig Star、穴口のBig Star、縦穴内Big Starは一定時間ターゲットから外し、スターと逆方向へ逃がす。`logs/codex-ruleai-luigi-deep-hole-star-avoid-20260614` では `seed=0xdeadbeef` のLuigi側host/client死亡が `2 -> 0` になり、スター取得は安全のため諦める挙動になった。
- 完了: Goomba/Koopa接近時の緊急ジャンプ範囲を広げた。SVG/PNGで `logs/codex-visual-remaining-ruleai-deaths-20260614` の `frame 1510-1540` を確認し、死亡は穴ではなくGoomba横接触で、trace上はhazard検出後もBが出ていないことを確認した。`kEnemyStompHorizontalRange` を `0x18000 -> 0x30000` に広げ、`logs/codex-ruleai-luigi-goomba-stomp-range-20260614` のLuigi `seed=0x13579bdf` 短縮traceではclient側Goomba死亡が `1 -> 0` になった。
- 完了: terrain grid由来の `FarHoleLeft/Right` をRuleAIへ渡し、床切れの向こう側にあるBig Star actor/candidateはターゲットから外すようにした。これにより、画面上では穴内に見えるスターを追い続けて落下するケースと、遠い床切れ越しスターに釣られてGoombaへ吸い込まれるケースを抑える。危険スター判定は「ターゲット禁止」と「逆方向へ逃げる」を分離し、穴口/縦穴内では逃げるが、安全な床に戻った後は逃げ入力を継続しない。`logs/codex-visual-mario-star-reject-cooldown-20260614` のPNGで `seed=0xdeadbeef` の穴スター位置とP0/P1の安全側移動を確認した。
- 検証: 最新の対称RuleAIで `logs/codex-ruleai-mario-farstar-regression2-20260614` はMario 3 seed x host/client平均死亡 `0.0`、平均生存率 `0.9506`。`logs/codex-ruleai-luigi-farstar-regression2-20260614` はLuigi 3 seed x host/client平均死亡 `0.0`、平均生存率 `0.9506`。どちらもstage 0 / seeds `0xdeadbeef,0x13579bdf,0x2468ace0` / 2400F / JITあり / game-state比較skipの閉ループ評価でpassした。旧 `logs/codex-ruleai-luigi-hole-star-goomba-regression-20260614` は `seed=0x2468ace0` 実行中にmelonDS timeoutでJSONLが途中破損したため、集計としては未採用。
- 完了: 穴中スター判定を目視/SVG/ログで再確認し、`logs/codex-visual-long-star-min-20260614` の死亡直前PNGではBig Star actorが縦穴内/穴底側にあり、壁キックなしで取ると復帰困難であることを確認した。RuleAIはtarget周辺のterrain grid中心列を使って「target直下に床があるか」を判定し、床に支えられていないBig Star actor/candidate/dropped starは追跡対象から外す。`logs/codex-visual-target-floor-20260614/luigi-host-f1940-p1.png` とtraceではframe 1940で穴中Big Starを `mode=starActor` にせず `mode=hold` になり、Mario/Luigiどちらも同じロジックのまま `logs/codex-ruleai-mario-target-floor-3600-20260614` / `logs/codex-ruleai-luigi-target-floor-3600-20260614` の3 seed x host/client 3600F評価で平均死亡 `0.0`、平均生存率 `0.9751` を確認した。安全優先のため同評価ではスター取得平均は `0.0` で、次は「床に支えられた安全スターだけ攻める」圧力を上げる。
- 完了: GUI/SVG上で通常Big Star actorと落としたBattle Starがどちらも `S` に見えていたため、`dropped_star_item` は `dS` として表示するように分けた。これにより、穴中に見える `S` が通常Big Starなのか、落としたスターなのかを目視監査で区別できる。
- 完了: `players[].tileProbe.samples[].block` を追加し、StageLayout上の現在tile id/behaviorから question block、breakable block、brick、invisible block、item box候補、storage contents、modifierを明示的に保存するようにした。CSV特徴量とinspect/SVG表示にも流す。
- 完了: stage 0実走で `feet/below/ahead*/left*/right*` の足元系offsetを少し下げ、RuleAI実操作経路のterrain traceを確認した。現在は17点raw probeの足元判定ではなく、terrain grid由来の床row推定で `groundBelowSolid` と `effectiveHole*` を作る。接地contactはgridが一時的に床を見失った場合の抑制にだけ使う。
- 完了: AI play logの `tileProbe.summary` に `contactGround`、`effectiveGroundBelowSolid`、`holeSuppressedByContact`、`effectiveHoleAhead/Left/Right` を追加した。raw tileProbeの `hole*` は残しつつ、接地contactを融合した実操作向け/学習向けの地形判断もCSV特徴量に入る。
- 完了: `tileProbe.samples[]` に `status` と未取得時のworld/pixel/chunk情報を保存し、`tile.lowType` を追加した。`tileId=0x001 behavior=0x0000002A` のような低位tile typeをCSVから直接集計できる。
- 完了: `scripts/nsmb_mvl_ai_catalog_tiles.py` を追加し、AI play logのtileProbeを sample/status/tile id/behavior/lowType/solidish/block/contact/effective ground別に集計できるようにした。
- 完了: 人間プレイログ収集に向けて、AI play logを記録単位で扱う `recording.json` / `recordings-index.json` のmanifest形式を追加した。`scripts/nsmb_mvl_ai_create_recording_manifest.py` で1ログの統計、最終状態、イベント候補、label sourceを保存し、`scripts/nsmb_mvl_ai_make_recordings_index.py` で複数記録を束ねる。
- 完了: `scripts/nsmb_mvl_ai_build_dataset.py` が JSONL単体だけでなく、`recording.json` と `recordings-index.json` を入力にできるようにした。CSVには `recording_index` / `recording_frame_index` を保存し、`scripts/nsmb_mvl_ai_train_imitation.py --split-by-recording` で記録単位のvalidation holdoutを選べる。
- 完了: `scripts/nsmb_mvl_ai_verify_replay.py` を追加し、melonDSで再実行したreplayログを、最終frame/hash/player座標/powerup/dead/star/coin/object countで検証できるようにした。デフォルトは完全一致で、必要時だけ位置許容やhash/object count無視を指定する。
- 完了: `scripts/nsmb_mvl_ai_export_viewer_data.py` を追加し、JSONLを外部ビューア向けのコンパクトJSONへ変換できるようにした。
- 完了: `scripts/run-nsmb-mvl-manual-local.ps1` に host/client別の `HostAIPlayLog` / `ClientAIPlayLog` を追加し、手動2窓プレイでもAI play logを別々に出せるようにした。
- 完了: `scripts/run-nsmb-mvl-human-recording.ps1` を追加した。stage 0固定で手動ローカル記録を開始し、終了後にmanifest/index/datasetを作る後処理コマンドを `recording-session.json` に残す。
- 完了: `recording.json` に `replay` / `metadata` / `quality` を追加し、入力script replayに必要なframes、host/client input script、ROM、match seed、scenario、品質ステータスを保存できるようにした。
- 完了: `scripts/run-nsmb-mvl-split-local-input-smoke.ps1` が host/client別AI play log出力先を受け取り、split local input replay中にもAI play logを保存できるようにした。
- 完了: `scripts/run-nsmb-mvl-recording-replay.ps1` を追加した。`recording.json` からstage 0 input script replayの起動計画を復元し、完了後に `nsmb_mvl_ai_verify_replay.py` で検証する。`-DryRun` で起動前の解決パスと検証対象を確認できる。
- 完了: `scripts/nsmb_mvl_ai_verify_replay.py` に任意のcheckpoint frame比較を追加した。`--checkpoint-interval` を指定すると、最終frameに加えて途中frameのhash、player状態、object countを比較できる。
- 完了: `recording.json` の `summary.eventSamples` に、star/coin/powerup/death/block/item/projectile候補の代表frameを保存できるようにした。件数だけでなく、目視確認すべきframeをmanifestから辿れる。
- 完了: Tauri GUIに `AIログ` タブを追加した。ローカルのJSONLまたはviewer JSONをファイル選択で読み込み、player中心のSVG相当相対配置、P0/P1入力、可視object数、イベント候補、カテゴリ数を確認できる。
- 完了: Tauri GUIの `AIログ` タブが `recording.json` の `summary.eventSamples` を読み込み、frame付きの記録イベントtimelineを表示できるようにした。manifest単体でも、死亡、block候補、item/projectile候補などの目視確認対象frameを確認できる。
- 完了: `scripts/run-nsmb-mvl-human-recording.ps1` が、通常の入力/AI play logに加えてpacket captureを有効化し、終了後に `packet-replay.csv` へ変換する後処理コマンドを `recording-session.json` に残すようにした。不要な場合は `-NoPacketCapture` で無効化できる。
- 完了: `recording.json` が `packetReplayFile` / `hostPacketReplayFile` / `clientPacketReplayFile` と `packetCapture.host/client` を保存し、`replay.mode=packet_replay` を扱えるようになった。
- 完了: `scripts/run-nsmb-mvl-recording-replay.ps1` が `input_script` と `packet_replay` の両方を解決し、packet replay時は `run-nsmb-mvl-split-local-input-smoke.ps1` へ host/client のpacket replay fileを渡すようにした。現時点の確認はdry-runで、実際の人間記録からの完全再現は次の検証対象。
- 完了: `scripts/nsmb_mvl_ai_verify_replay.py --scan-frames` で全記録frameを比較し、最初にズレたframeを検出できるようにした。`--mismatch-report-json` / `--mismatch-report-csv` を指定すると、viewer/表確認向けの差分reportを保存する。
- 完了: `scripts/run-nsmb-mvl-recording-replay.ps1 -ScanFrames` が replay検証時に `replay-mismatch.json` / `replay-mismatch.csv` を出力する起動計画を作れるようになった。
- 完了: `scripts/run-nsmb-mvl-recording-postcommands.ps1` を追加し、`recording-session.json` に残した後処理コマンドをdry-run/途中再開つきで実行できるようにした。
- 完了: AI play logに `specialObjects.fireballs` / `specialObjects.projectiles` を追加した。通常actor object listでは拾えない `Fireballs::activeFireballs` と fireball/projectile handler先頭wordを保存し、Fire Mario実ログで発射frameを見つける足場にする。
- 完了: `specialObjects.fireballs.active` をdataset特徴量、viewer export、recording manifestの `summary.specialObjectFrames.fireballActive` / `eventSamples.fireballActive`、GUIの `AIログ` タブへ流すようにした。
- 完了: `Fireballs::fireballHandler` のslot配列を読み、AI play logの `specialObjects.fireballs.slots[]` にactive slotのindex、kind、state、facing、座標、前フレーム座標、速度、player相対座標を保存するようにした。datasetには `fireballs_active_slots`、`fireballs_slot_count`、最近傍fireballの相対距離/kind/state/facingを追加した。
- 完了: `specialObjects.fireballs.slots[]` にactive slotの `stateBytes` (`+0x80` から12 bytes) と `debugWords` (`+0x40` から16 words) を追加した。lifetime/当たり判定状態はまだ未検証なので、Fire Mario実ログで値変化を画面と照合するためのraw監査情報として扱う。datasetには近傍fireballの代表byte/wordを追加した。
- 完了: AI play logの `players[].visualState` に、現在powerupとreserve itemのraw値、暫定名、Fire/Shell/Mega候補、reserve item候補、damage guard timer/cooldown/flag、physics guard、甲羅stateを保存するようにした。powerup enum mappingは未検証なので `mappingVerified=0` として残す。
- 完了: `specialObjects.fireballs.slots[]` に `ownerCandidate`、`ownerConfidence`、`ownerHeuristic`、`ownerVerified` を追加した。ROM逆アセンブルで `Fireball::create` がspawn kindをslot `+0x81` へ保存し、player fireballではkind `0/1` がowner player id、kind `2/3` が敵由来fireballであることを確認した。新規ログでは `sourceKind`、`kindName`、`ownerSource=slotKind`、`ownerVerified=1` を出す。旧ログでも監査/dataset/SVG側は `kind` からownerを補正する。
- 完了: `players[].visualState` に `visualPowerupKindCandidate`、`visualPowerupSourceMask`、`isFireVisualCandidate`、`canShootFireVisualCandidate` を追加した。`powerup.raw` が0固定でも、player actor内の変身状態から画面上のFire候補をdataset/manifest/SVG/監査へ流せる。
- 完了: `specialObjects.fireballs.slots[]` のowner推定をslot単位で追跡するようにした。これはkind不明の将来projectile向けfallbackとして残す。player/enemy fireballでは `sourceKind` が検証済みなので、位置/速度ベース推定ではなくslot `+0x81` を優先する。
- 完了: datasetに self/opponent 両方の `inventoryPowerup`、powerup候補bit、invincibility候補、damage guard timer/cooldown/flag、shell state、相手のaction/subAction/physics/collision/environment/dead、最近傍fireball owner候補を追加した。ログに出ているのに学習入力へ落ちていなかった相手powerup/deadもCSVへ入る。
- 完了: `scripts/nsmb_mvl_ai_audit_visual_state.py` を追加した。playlogから `visualState` 欠落、invincibility未確定、damage guard/shell観測frame、powerup raw値分布、可視未知object、fireball owner低信頼slotを集計し、人間が画面で見える状態に対するログ不足を検出する。
- 完了: `scripts/nsmb_mvl_ai_audit_fireballs.py` を追加した。`specialObjects.fireballs.slots[]` からslotごとの発生区間、source kind/kindName、owner候補、owner verified、state/facing分布、移動サンプル数を集計し、Fire Marioログでfireball状態が学習特徴量として使えるかを確認できる。
- 完了: `scripts/nsmb_mvl_ai_audit_items.py` を追加した。AI play log内のitem系actorを `objectId/settings/vtable/category` ごとにクラスタリングし、出現frame、可視frame、playerとの最短距離、近傍で起きたcoin/powerup/invincibilityなどの状態変化、代表SVGを出力できる。`scripts/run-nsmb-mvl-item-audit-sweep.ps1` ではseed、policy、P0/P1のBattle Star差を振ってclosed-loop実走後にitem監査を自動実行できる。
- 完了: RuleAIのhazard選択に `specialObjects.fireballs.slots[]` を統合した。自分のfireballは除外し、相手playerまたは敵由来fireballはobject scanのenemy/hazardと同じ `HazardFound/Dx/Dy/Vel/Closing/VeryClose` へ流す。Fire Mario相手の人間ログ収集で、通常actor list外のfireballをRuleAIが無視し続ける状態は避ける。
- 完了: 検証用にactive powerupを固定する `MELONDS_NSML_FORCE_PLAYER_POWERUPS` を追加した。`POWERUP0/1=2` を指定するとglobal powerupとplayer actor内のpowerup/form stateをFire候補へ固定し、`run-nsmb-mvl-ai-closed-loop-eval.ps1`、batch、manual/human recording系スクリプトから `-ForcePlayerPowerups` として渡せる。通常起動では無効で、Fire Mario開始scenarioの状態取得/ログ収集検証に使う。
- 完了: SVGレンダラに playerの `visualState` 表示と `specialObjects.fireballs.slots[]` の描画を追加した。fireballはowner候補とconfidenceをtitleに出す。
- 完了: 人間記録後処理に `visual-state-audit.json` と `fireball-audit.json` 生成を追加した。`recording-session.json` のpostCommandsでmanifest/index/compact datasetに続けて視覚状態監査とfireball slot監査を実行する。従来CSV datasetとCSV terrain auditは `-BuildLegacyDataset` 指定時だけ作る。
- 完了: split local input smokeの `HostAIPlayLog` / `ClientAIPlayLog` 相対パスをrepo root基準の絶対パスに解決するようにし、child側LogDirとの二重パス化を避けるようにした。
- 完了: `scripts/nsmb_mvl_ai_audit_recordings.py` を追加した。`recording.json` または `recordings-index.json` を読み、rows、gameplay rows、player found率、label率、nonzero label、stage、packet replay、必須event数を学習前に検査できる。
- 完了: `scripts/run-nsmb-mvl-human-recording.ps1 -DryRun` を追加し、melonDSを起動せずに `recording-session.json` と後処理コマンドを確認できるようにした。通常後処理には `recording-audit.json` 生成も含める。
- 完了: `scripts/run-nsmb-mvl-human-recording.ps1 -OpponentAI rule` を追加した。既定のclient/Luigi手動記録ではMario側をRuleAI対象 (`MARIO`) にし、`recording-session.json` へ `opponentAI` / `opponentAIPlayer` を保存する。`-ForcePlayerPowerups -ForcePlayerPowerup0 2 -ForcePlayerPowerup1 2` と組み合わせると、Fire Mario/Fire Luigi開始の人間ログ収集を同じ入口から起動できる。
- 完了: 手動2窓記録で片側の物理入力だけを無効化できるようにした。`MELONDS_NSML_NEUTRALIZE_POLLED_INPUT=1` は物理コントローラ/キー入力をニュートラルにするが、試合開始用input scriptは有効なまま残す。`scripts/run-nsmb-mvl-manual-local.ps1` には `-NeutralizeHostInput` / `-NeutralizeClientInput` を追加し、`scripts/run-nsmb-mvl-human-recording.ps1` はデフォルト `HumanSide=client` としてhost/Mario側をニュートラル化する。
- 完了: `scripts/run-nsmb-mvl-human-recording.ps1` から `run-nsmb-mvl-manual-local.ps1` を呼ぶ引数渡しをhashtable splattingへ変更した。文字列配列splattingで `-Frames` が位置引数扱いになり、`Frames` に `"-Frames"` が入って変換エラーになる問題を修正した。
- 完了: `scripts/nsmb_mvl_ai_predict_input_script.py` を追加した。学習済み `.npz` とAI play log / recording manifest / recordings indexから推論し、melonDSのinput scriptへ戻せる。既存bootstrap scriptの前置き、反応遅延、ランダムミス、予測CSV出力に対応する。
- 完了: `scripts/run-nsmb-mvl-split-local-input-smoke.ps1` に `-AllowRemoteInputTimeoutFallback` と `-InternalWaitTimeoutMs` を追加し、モデルinput script評価時にremote input timeoutを即時fallbackへ近づけられるようにした。
- 完了: 手動人間記録の既定を、2窓host/client同期ではなく `single_client_authoritative` に変更した。`run-nsmb-mvl-human-recording.ps1` はデフォルトでclient/Luigi側1窓だけを起動し、その1つのmelonDSインスタンスからMario/Luigi/敵/アイテム/地形/入力ラベルを同一frameの権威ログとして保存する。2窓同期は `-DualWindow` の実験オプションに残すが、現時点ではpeer接続待ちと入力遅延が実プレイ記録に不適なので学習ログ既定には使わない。
- 完了: client-only手動記録は入力遅延を `InputDelayFrames=0` にし、JITを既定で有効にする。`logs/codex-human-recording-single-client-scripted-20260608` では2200Fを約55.5fpsで完走し、dataset 1332行、nonzeroLabelRows 1300、`recording-audit.json` passを確認した。無操作ログは `nonzeroLabelRows=0` で監査failになり、学習用に無入力記録を弾ける。
- 完了: AI play logは学習内容を変えない範囲でflush頻度を調整できるようにした。`MELONDS_NSML_AI_PLAY_LOG_FLUSH_INTERVAL=60` が既定で、JSONLの各行内容は変えずにofstream flushだけを60行ごとにまとめ、終了時に明示flushする。固定seed `0x12345678`、2200F、`AIPlayLogInterval=1`、`AIPlayLogMaxObjects=128` の比較では、`flushInterval=1` が active fps 53.52 / 25ms超44frame、`flushInterval=60` が active fps 55.75 / 25ms超12frame。両者の `ai-playlog.jsonl` は1410行、52,779,281 bytes、SHA256 `DB3A75B422241D06407748D50010EA13073430EAF46072645844C52BDC87D817` で完全一致した。
- 完了: RuleAIの壁/進捗なし監査を `scripts/nsmb_mvl_ai_audit_ruleai.py` として追加した。死亡遷移、壁方向入力、穴方向入力、左右反転、停滞区間、接地/effective ground矛盾をJSONで出せる。旧 `logs/watch-ruleai-2/client/ai-playlog.jsonl` では死亡1、壁方向入力66、停滞1を検出し、修正対象を機械的に拾えることを確認した。
- 完了: closed-loop評価からPNGスクショを取れるように、`run-nsmb-mvl-ai-closed-loop-eval.ps1 -ScreenshotInterval <n> -SoftwareRenderer` と `run-nsmb-mvl-split-local-input-smoke.ps1 -ScreenshotInterval <n> -SoftwareRenderer` を追加した。OpenGL computeではテストフック側の `GPU.GetFramebuffers()` が `no-framebuffer` になるため、スクショ評価ではsoftware rendererを使う。`MELONDS_NSML_SCREENSHOT_REG_TRACE=1` で未保存理由を出せる。
- 完了: `scripts/nsmb_mvl_ai_render_eval_events.py` は、評価JSON内の `logs\...` repo相対入力を現在cwdからも解決するようにした。既存評価JSONからイベントSVGが0件になるケースを避ける。
- 完了: RuleAIに短期記憶ベースの壁脱出を追加した。目標方向が壁/穴へ向く場合は一時的に反対方向またはraw座標経路へ逃がし、死亡中は中立入力にして短期記憶をリセットする。traceには `dx/rawDx/intent/escape/still` を出す。
- 検証: `logs/codex-ruleai-safe-revert-3300-20260613` は stage 0 / seed `0x2f52869f` / 3300F / software renderer / screenshot 300F間隔でpass。host/clientともPNG 11枚を生成し、client代表 `inst0_frame003300.png` は草原ステージ画面として目視確認済み。closed-loop評価では client `deathTransitions=0`、`aliveRows=82/84`、`bigStarDistance.approachDelta=469225177088`。
- 解決済み調査: `logs/codex-ruleai-safe-revert-3300-20260613` のframe 1290-1500付近で、旧ログの `tileProbe.summary.wallRight=1` はbody側壁ではなく `rightFeet` 側の `tile=0x019 behavior=0x00080002` を横壁として拾ったfalse positiveだった。スクショとtileProbe座標を重ねると、このtileは階段状に見えている通常のbreakable blockと一致し、別レイヤーの隠し/救済blockとして取れている証拠はない。dataset再生成後は `self_tile_probe_wallRight=0`、`self_tile_probe_raw_wallRight=1`、`self_tile_probe_wallRight_recomputed_diff=1`、`self_tile_probe_rightFeet_block_hiddenOrRescueCandidate=0`、`self_tile_probe_rightFeet_block_visibleStorageBreakableCandidate=1` になる。
- 完了: RuleAI監査に `oscillationWindows` を追加した。左右入力を切り替えても狭い座標範囲に留まる振動スタックを検出する。`logs/codex-ruleai-wallprobe-3300-20260613` の試作では `stuckWindows=0` になったが、スクショではブロック近辺に留まり、監査v2で `oscillationWindows=11` を検出したため採用しなかった。
- 完了: RuleAI監査に `hazardNearRows` / `hazardDeathTransitions` を追加した。死亡直前の最近傍 `moving_hazard` / enemyを保存し、地形スタックを減らす代わりにシェル/敵接触死を増やす試作を自動で弾く。`logs/codex-ruleai-safe-revert-3300-20260613` は `hazardDeathTransitions=0`、失敗試作 `logs/codex-ruleai-hazardjumpmem-2100-20260613/client` は `hazardDeathTransitions=1` を検出した。
- 完了: RuleAI監査に `bodySolidRows` / `itemBoxBodyRows` を追加した。`logs/codex-ruleai-safe-revert-3300-20260613/client` では `bodySolidRows=69`、`itemBoxBodyRows=69` で、frame 1290以降の長時間スタックを機械的に検出できる。ただし `itemBoxBodyRows` は `storageContents != 0` のblock tileを暫定的にitemBox扱いしている名前で、画面上の可視ブロックへの食い込みをそのまま意味しない。
- 完了: `scripts/run-nsmb-mvl-ai-closed-loop-eval.ps1 -Policy rule` は評価後に host/client それぞれの `ruleai-audit.json` を自動生成する。`-RuleAIAuditSampleLimit` と `-RuleAIAuditStuckRecords` でサンプル数と停滞判定窓を調整できる。
- 調査結果: frame 1500のSVG/PNG overlayでは、selected playerの `center/feet/left/rightFeet` が `tile=0x019 behavior=0x00080002` と重なる。これは `breakableBlock=1` / `invisibleBlock=0` / `lowType=2` の見えている階段状blockで、旧 `storageContents=2` は実アイテム中身ではなくbehavior低位bitsを拾ったlegacy値として扱う。スクショでは右側が空いているのにsummaryだけ `wallRight=1` になっていたため、見た目上の地形とAI用地形summaryがズレていた。hazard検知範囲拡大、近距離踏みモード、tileProbe center/feet solid時のbodyEscape、壁際hazard hop、危険時Aパルス/クールダウン、両壁時にraw目標方向へ逃がす修正も試したが、シェル/Goomba接触死、落下死、または同地点スタック継続が出たため不採用。
- 完了: tileProbe summary / RuntimePlayerFeature / imitation hazard guard / RuleAI入力では、左右壁判定から `leftFeet/rightFeet` 単独のsolidishを外し、body側probeまたはCollisionMgrの左右接触だけで `wallLeft/wallRight` を立てるようにした。これにより、足元のstorage blockを横壁として拾うfalse positiveを抑えた。
- 完了: `scripts/nsmb_mvl_ai_build_dataset.py` は、playlog内の古い `players[].tileProbe.summary` をそのまま学習列へ入れず、`samples` と `contact` からC++ runtimeと同じ正規化summaryを再計算するようにした。旧summaryは `*_tile_probe_raw_*`、差分は `*_tile_probe_*_recomputed_diff` として監査用に残す。これにより、過去に収集した人間ログを再dataset化しても、foot側storage block由来の `wallLeft/wallRight` false positiveは学習入力に入らない。
- 完了: datasetへ sample別の `*_block_storageBreakableCandidate` / `*_block_hiddenOrRescueCandidate` / `*_block_visibleStorageBreakableCandidate` / `*_block_visibleSolidCandidate` を追加した。`hiddenOrRescueCandidate` は `invisibleBlock` が立つ場合に限定し、`tile=0x019 behavior=0x00080002` のような見えているbreakable階段blockは `visibleStorageBreakableCandidate` として扱う。旧 `storageContents` / `itemBox` はbehavior低位bitsを含むlegacy値なので、隠し/救済判定には単独で使わない。
- 完了: `scripts/nsmb_mvl_ai_audit_dataset_terrain.py` を追加した。dataset CSVに対して、footだけで立つ横壁イベント、raw summaryからの壁補正、隠し/救済block候補をJSONで監査する。
- 完了: 17点の近接tileProbeだけでは階段状の地形全体を目視同等に表現できないため、`players[].tileProbe.grid` を追加した。gridはplayer tile基準の横33 x 縦17、相対範囲は `relTileX=-16..+16`、`relTileY=-10..+6`。各cellは `row/col/relTileX/relTileY/tileX/tileY/pixelX/pixelY/status/tileId/behavior/tile/block/solidish` をcompactに保存する。datasetには `*_tile_probe_grid_r<row>_c<col>_*` として固定列展開する。これはDS画面の16x12 tileより広い視野を取り、先の地形を点ではなく面として学習へ渡すためのもの。
- 完了: `players[].tileProbe.grid` のplaylog保存を `encoding=sparse_non_empty` に変更した。全561 cellを毎回JSONへ出すのではなく、solidish/block/coin/water/harmful/entrance/climbable/partial solidなど意味のあるcellと、未取得status cellだけを `cells[]` に保存する。省略cellは `omittedCellFound=1` / `omittedCellStatus=0` のvalid emptyとしてdataset生成時にdense gridへ復元する。
- 検証: `logs/human-stage0-item-box-001/ai-dataset-player1.csv` の旧dataset監査では self側 `sideWallFeetOnlyEvents=731`。同じ `client/ai-playlog.jsonl` を新builderで `logs/codex-terrain-dataset-rebuild-item-box-001-20260613-v2.csv` へ再生成すると `sideWallFeetOnlyEvents=0`、`rawWallClearedEvents=731`、`hiddenOrRescueCandidateRows=0`、`visibleStorageBreakableCandidateRows=521`。旧ログ由来の足元横壁false positiveは学習用wall列から消え、見えているbreakable block候補として別列に残る。
- 検証: `logs/codex-ruleai-safe-revert-3300-20260613/client/ai-playlog.jsonl` を新builderで再dataset化すると、self側 `sideWallFeetOnlyEvents=0`、`rawWallClearedEvents=69`、`hiddenOrRescueCandidateRows=0`、`visibleStorageBreakableCandidateRows=70`。frame 1500のスクショでは右側が空いており、CSVでも `self_tile_probe_wallRight=0`、`self_tile_probe_raw_wallRight=1`、`self_tile_probe_rightFeet_tile_id=25`、`self_tile_probe_rightFeet_block_hiddenOrRescueCandidate=0`、`self_tile_probe_rightFeet_block_visibleStorageBreakableCandidate=1` を確認した。
- 検証: `logs/codex-tile-grid-ruleai-1600-20260613` を同seed `0x2f52869f` / RuleAI / 1600Fで実走し、new grid付きAI play logを確認した。client frame 1500のgrid ASCIIでは、左側階段が `xx` / `xxx` / `xxxx` / `xxxxx` と面で取れている。`python scripts\nsmb_mvl_ai_build_dataset.py ...` で `ai-dataset-player1-grid.csv` を生成し、総列数34495、grid列32538を確認。`dataset-terrain-audit-grid.json` は `sideWallFeetOnlyEvents=0`、`hiddenOrRescueCandidateRows=0`、`visibleStorageBreakableCandidateRows=9`。
- 検証: sparse化後の `logs/codex-tile-grid-sparse-ruleai-1600-20260613/client/ai-playlog.jsonl` は27行で2.86MiB、約110.9KB/行。dense grid版の `logs/codex-tile-grid-ruleai-1600-20260613/client/ai-playlog.jsonl` は27行で20.42MiB、約792.9KB/行だったため、同条件で約86%削減。gridなし旧ログは約36.8KB/行なので、sparse grid付きでも地形視野を広げたぶんまだ約3倍重い。sparseログからdataset復元とterrain auditはpassし、`visibleStorageBreakableCandidateRows=9` を維持した。
- 検証: `logs/codex-tile-grid-sparse-ruleai-1600-20260613/client/block-type-overlay-f1500-player1-corrected.png` を生成し、client frame 1500の実スクショへplayer1の `tileProbe.grid` block種別を重ねた。最初の確認画像は `pixelX % 256` で画面外blockを誤って画面内へ重ねる可視化ミスがあったため破棄し、`screenX = (worldX - cameraX) mod 1024` で補正した。補正後の可視範囲内描画セルは `question=1`、`breakable=6`、`brick=0`、`hidden=0`、`visibleStorageBreakable=6`。右側のquestion/item blockと左側の階段状breakable blockはスクショ上の見た目と対応し、同フレームでは隠し/invisible候補は出ていない。
- 完了: RuleAIに `rawWallRoute`、`guardLead`、`guardBrake`、`airRecover` を追加した。wrap距離で壁へ向かう時はraw座標方向が開いていればそちらへ回し、スター優勢時は次スター追跡を止め、速度と逆方向へ短くブレーキする。空中でスター優勢かつ穴判定が出た場合は片側壁から離れる方向へ復帰を試す。
- 完了: RuleAIは、Big Star actor/candidateがまだ見えている間はスター優勢でも `guardLead/guardBrake` へ入りすぎず、次のstar routeを追い続けるようにした。また片側だけ穴判定が出て反対側が開いている場合は `holeAvoid` として短く反転する。これは人間ログ収集相手として「スターを取った後に止まりすぎる」「片側穴へそのまま入る」挙動を減らすための限定修正。
- 検証: `logs/codex-ruleai-guardbrake-3300-20260613` は stage 0 / seed `0x2f52869f` / 3300F / software rendererでhost/clientとも `deathTransitions=0`、それぞれ `starPickups=1`。同seedの旧safeログで残っていたclientの長時間左壁スタックは解消し、client `blockedInputRows=1`、`stuckWindows=0` まで減った。
- 検証: `logs/codex-ruleai-guardbrake-3seed-2600-20260613` は seeds `0x2f52869f,0x13579bdf,0x12345678` / 2600Fで `avgDeathTransitions=0.5`、`avgAliveRatio=0.925`、`avgStarPickups=0.333`、`avgBigStarApproachDelta=462303582890.6667`。以前の5seed記録 `avgDeathTransitions=1.0` / `avgAliveRatio=0.8833333333` より安全側に改善したが、seed `0x13579bdf` と `0x12345678` ではまだ死亡が残る。
- 検証: `logs/codex-ruleai-star-hole-3seed-2600-20260613` は stage 0 / seeds `0x2f52869f,0x12345678,0x13579bdf` / 2600F / RuleAIでpass。aggregateは `avgDeathTransitions=0.3333333333`、`avgAliveRatio=0.9388888889`、`avgStarPickups=0.3333333333`。host/client片側ごとの差は残るが、採用済みのstar追跡継続と片側穴回避で死亡平均は下がった。
- 検証: `logs/codex-ruleai-fire-start-smoke-20260613` と `logs/codex-ruleai-fire-start-screens-20260613` で `-ForcePlayerPowerups -ForcePlayerPowerup0 2 -ForcePlayerPowerup1 2` を指定し、powerup/visualPowerupが両playerとも2、Fire表示スクショ、fireballActive発生を確認した。`logs/codex-ruleai-fire-start-smoke-20260613/fireball-audit.json` は active slot sample 147、owner verified 147、low confidence 0。
- 検証: `logs/codex-ruleai-fire-start-2seed-1800-20260613` は Fire開始RuleAIを2seedで評価。`0x13579bdf` はhost/clientとも `deathTransitions=0`、`0x2f52869f` はhost/clientとも死亡が残る。fireball監査は4ログ合計 active slot sample 14、owner verified 14、low confidence 0。Fire scenarioは状態取得とownerは使えるが、RuleAIの落下/地形回避はまだ十分ではない。
- 不採用: `hazardJumpOver` 試作は `logs/codex-ruleai-hazard-jumpover-13579bdf-2250-20260613` でGoomba接触死が消えず、client側死亡も増えたため戻した。壁際でGoombaが近づく場合の正解は単純な「相手方向へジャンプ」ではなく、もっと早い進路変更、踏みタイミング、またはFire開始で倒すなどscenario別に分けて扱う。
- 完了: RuleAI hazardの `VeryClose` 判定を水平hazard rangeの1/2から3/4へ広げた。`logs/codex-ruleai-hazardclose-3seed-2600-20260613` は同3seed / 2600Fで `avgDeathTransitions=0.1666666667`、`avgAliveRatio=0.9527777778`、`avgStarPickups=0.333`。seed `0x13579bdf` clientと `0x12345678` host/clientの死亡は消えた一方、seed `0x12345678` hostのBig Star接近は悪化したため、次は安全を維持したままstar routeを戻す。
- 完了: datasetとC++逐次推論feature resolverに `runtime_hazard_*` を追加した。`moving_hazard` / `hazard` / `enemy_goomba` / `enemy_koopa` を統合して、RuleAI/Imitation hazard guard相当の最近危険対象について `found`、`closing`、`very_close`、相対位置、速度、距離、category、object id、settingsをCSV特徴量へ保存する。これにより、seed `0x13579bdf` のGoomba接触死直前のような「画面上の敵がこちらへ近づいている」状態を、個別object最近傍だけでなく学習しやすい派生特徴として扱える。
- 不採用: seed `0x12345678` hostのスター接近悪化に対して、非接近かつ同方向hazardでは逃げずにjumpだけ足す `hazardJump` 試作を試したが、`logs/codex-ruleai-hazardjump-route-2seed-2600-20260613` で seed `0x13579bdf` のhost/client死亡が戻り、seed `0x12345678` hostのBig Star接近も改善しなかったため戻した。現時点ではRuleAI挙動を強引に直すより、hazard特徴量を模倣学習・DAgger向けに残し、RuleAI相手ログで人間がどう回避するかを学習できる状態にする方が優先。

## AI Play Log

有効化env:

- `MELONDS_NSML_AI_PLAY_LOG=<path>`: JSONL出力先。
- `MELONDS_NSML_AI_PLAY_LOG_INTERVAL=1`: ログ間隔。学習データ収集は1、smoke/目視検査は30などでよい。
- `MELONDS_NSML_AI_PLAY_LOG_FLUSH_INTERVAL=60`: JSONLを何行ごとにflushするか。0なら終了時flushのみ。ログ内容は変えず、実プレイ時のディスクI/Oスパイクだけを抑えるための設定。異常終了時は最後の最大N行がOS/iostream bufferに残る可能性があるため、重要な手動記録ではmelonDSを正常終了させる。
- `MELONDS_NSML_AI_PLAY_LOG_START_FRAME=0`
- `MELONDS_NSML_AI_PLAY_LOG_END_FRAME=0`: 0なら終了指定なし。
- `MELONDS_NSML_AI_PLAY_LOG_MAX_OBJECTS=32`: 1フレームに保存するactive object上限。
- `MELONDS_NSML_AI_PLAY_LOG_INCLUDE_NON_GAMEPLAY=1`: 通常はMvL gameplay中だけ保存する。これを指定すると非gameplayも含める。

JSONL schema `nsmb_mvl_ai_play_log_v1` は、各行に `inputs`、`players`、`targets`、`camera`、`objectSummary`、`specialObjects`、`objects` を持つ。`objects` はactive objectだけを保存し、既知IDには `category` を付ける。各objectには `objectId`、`settings`、`guid`、`base`、`offset`、`vtable`、state/flags、座標、速度、player相対座標、screen情報を保存する。

`inputs` にはメモリ上の `console0/1`、`player0/1` に加えて、PoCが実際にそのフレーム近辺へ注入した `appliedPlayer0/1` を保存する。模倣学習の教師ラベルはまず `appliedPlayerN.held` / `heldHex` を使う。

`players[].contact` には、数値の衝突/環境フラグから人間が目視で判断する地形接触に近いbitを保存する。代表項目は `ground`、`predictGround`、`ceiling`、`wallLeft`、`wallRight`、`edgeGrab`、`water`、`liquid`、`submerged`、`quicksand`、`rope`、`tightrope`、`pole`、`spikesLeft`、`spikesRight`、`conveyorLeft`、`conveyorRight`、`wrapLeft`、`wrapRight`。CSVには `self_contact_*` / `opponent_contact_*` として展開する。

`players[].visualState` には、画面上の変身状態やストック表示に相当する候補を保存する。`powerup.raw` / `inventoryPowerup.raw` はメモリから読んだ値で、`name` は暫定mapping、`mappingVerified=0` はstage 0実ログで値と画面表示をまだ照合していないことを示す。`canShootFireCandidate`、`isMiniCandidate`、`isShellCandidate`、`isMegaCandidate`、`hasReserveItemCandidate` はraw値からの候補bitであり、模倣学習では使えるが、評価時は `mappingVerified` を見て未検証値を過信しない。`visualPowerupKindCandidate` は現在の身体状態だけを表す。reserve itemやスター無敵は `hasReserveItemCandidate` / `inventoryPowerup` / `starInvincibleCandidate` として別に残し、通常状態をスーパー/ファイア状態と誤判定しない。Fireはactor state/form state 2、巨大はactor state/form state 3、マメはactor state/form state 4、スーパーはactor state/form state 1、甲羅はshell state、actor state/form state 5、またはraw powerup 4を優先して判定する。2026-06-14ログでは、Luigiがスター無敵中のframe 22262で `inventoryPowerup=2` かつ actor state/form/shell が0だったため、C++ログ出力とdataset/SVG/visual auditではこの組み合わせを `starInvincibleCandidate=1` として扱い、`invincibleCandidate` にも含める。`visualPowerupSourceMask` は現在の身体状態の根拠bitで、reserve/inventory単独は含めない。`isFireVisualCandidate` / `isMiniVisualCandidate` / `isMegaVisualCandidate` は画面上のFire/マメ/巨大候補、`canShootFireVisualCandidate` は現在変身状態から見てファイアーボールを撃てる候補を示す。`damageGuardTimer` は `0x0208B344 + player*2` の16bit値、`damageCooldown` はplayer actor `+0x79C`、`damageGuardFlag` は `+0x7C1`、`damagePhysicsGuard` は `physicsFlag & 0x80000000`、`shellState` は `PlayerBase::getShellState` と同じ `actionFlag & 0x400000` と shell actor pointerから計算する。`invincibleKnown=1` はこれらのdamage guard系ソースとstar candidateをログ化できていることを示す。スター無敵とダメージ後無敵は `starInvincibleCandidate` / `damageInvulnerableCandidate` で原因を分ける。

`players[].collisionMgr` には、プレイヤーactor内のCollisionMgrから読んだ地形/接触結果を保存する。主な項目は `collisionResult`、`groundCollision`、`deltaX/Y`、`attachedTileX/Y`、`bottomModifierTileType`、`topModifierTileType`、`sideModifierTileTypeLeft/Right`。`bottomModifierTile` は modifier tile typeを `solid`、`coin`、`questionBlock`、`breakableBlock`、`brickBlock`、`slope`、`water`、`partialSolid`、`harmful`、`invisibleBlock`、modifier、storage contentsへ展開したもの。これは「足元そのもの」ではなく、CollisionMgrが保持するbottom modifier tileで、前方タイルや穴の完全判定ではない。CSV/inspectでは不自然に多数のtile category bitが立つ値をsanity checkで除外する。

`players[].tileDamage` には、Player本体に保存されるtile damage flags/typeを保存する。`active=1` のときはlava/poison/その他ダメージ地形などの接触候補として扱う。通常の床接地や壁接触は `contact` と `collisionMgr.collisionResult` を見る。

`players[].tileProbe` には、StageLayoutから直接読んだタイル地形サンプルを保存する。サンプル点は `center`、`feet`、`below`、`aheadBody`、`aheadFeet`、`aheadBelow`、`ahead2Feet`、`ahead2Below`、`above`、`leftBody`、`leftFeet`、`leftBelow`、`left2Below`、`rightBody`、`rightFeet`、`rightBelow`、`right2Below`。各点は `status`、`pixelX/Y`、`chunkId`、`tileId`、`behavior`、`tile`、`block`、`solidish` を持つ。加えて `grid` はplayer tile基準の横33 x 縦17 cellで、画面相当以上の周辺地形を連続tileとして保存する。grid cellは `row/col/relTileX/relTileY/tileX/tileY/pixelX/pixelY/status/tileId/behavior/tile/block/solidish` を持ち、v2では `terrain.encoding=sparse_channel_mask_v2` として `solid/coin/question/breakable/brick/slope/scanSolid/entrance/water/partialSolid/harmful/invisible/itemBox/hiddenOrRescue/visibleStorageBreakable/visibleSolid` のbit channelへ圧縮する。`status=0` は取得成功、それ以外はStageLayout/chunk/behavior tableなどの未取得理由を示す。未取得時もworld/pixel/chunk候補は残す。`tile` は `solid`、`scanSolid`、`entrance`、`coin`、`questionBlock`、`breakableBlock`、`brickBlock`、`slope`、`water`、`partialSolid`、`harmful`、`lowType` などへ展開する。`block` は現在のStageLayout tileから `any`、`itemBox`、`question`、`breakable`、`brick`、`invisible`、`storageContents`、`modifier`、`currentTileId`、`currentBehavior` を保存する。`storageContents` / `itemBox` はbehavior低位bitsを含むlegacy値で、実アイテム中身や隠し/救済判定として単独では使わない。

`tileSummary.source=terrain_grid` の短縮地形特徴は、17点probeではなく `33x17` gridから導く。具体的には、grid内のplayer近傍から足元床rowを推定し、その床rowより上を身体高さとして `blockedAhead/Left/Right`、床rowから下を前方床として `holeAhead/Left/Right` を作る。`effectiveHole*` はgrid穴判定を基本にし、接地contactがあるのに一時的に床が見つからない場合だけ抑制する。隠し/救済候補やinvisible blockは短縮solidから外し、`hiddenOrRescue` channelとして別に残す。17点probe由来の `aheadBodySolid` などは監査用に残し、`probeGroundBelowSolid`、`probeBlocked*`、`probeHole*` で差分確認できる。CSV生成時も旧ログ内のraw summaryをそのまま使わず、`grid` / `terrain` から再計算する。旧summaryは `*_tile_probe_raw_*`、差分は `*_tile_probe_*_recomputed_diff` として監査用に残す。CSVにはさらに `*_block_storageBreakableCandidate` / `*_block_hiddenOrRescueCandidate` / `*_block_visibleStorageBreakableCandidate` / `*_block_visibleSolidCandidate` を出し、見えているbreakable blockと隠し/救済候補を分離する。

`players[].screen` / `players[].fallRisk` には、playerをカメラ座標へ投影した情報を保存する。`fallRisk` は `screenY0/1`、`cameraBottomDistance0/1`、`nearCameraBottom0/1`、`belowCamera0/1`、`velYPositive/Negative` を持つ。完全な穴判定ではないが、目視上の「下へ落ちている」「画面下端に近い」を学習データに入れるための暫定特徴。

`visualSummary` には、人間が画面を見て判断する情報に近づけるための要約を保存する。現時点ではY込みの `visibleCamera0/1`、左右ラップ込みX判定の `visibleCamera0X` / `visibleCamera1X`、カテゴリ別count、player別最近傍 `big_star_actor` / `enemy_goomba` / `enemy_koopa` / `moving_hazard` などを持つ。player/object個別にも `relative` と `screen.camera*.inViewX/inViewY/inView` を保存する。

可視object分類では、マップ上の通常Big Starは `big_star_actor` を学習用ターゲットにする。`big_star_related` と `big_star_marker` は内部補助actorとして表示/最近傍特徴から外す。旧ログに `dropped_star_item` として残っている `objectId=0x01F settings=0x00090002` は箱から出る取得コインだったため、dataset/SVG/GUIでは `coin_item` へ正規化する。一方、player死亡などで落ちたスターは `objectId=0x022` のスターactor variantで、settingsの高bitを除いた値が `0x1002`、`0x1012`、`0x1102`、`0x1112` のものを `dropped_star_item` として扱う。これは `logs\nsmb-mvl-human-recording-stage0-20260613-203615\client\ai-playlog.jsonl` frame 1348 の実ログで確認した。

`specialObjects` には、通常のactor object list外で管理されるものを保存する。現時点では `fireballs.active`、`fireballs.activeSlots`、`fireballs.handler`、`fireballs.handlerPtr`、`fireballs.words[]`、`fireballs.slots[]`、`projectiles.handler`、`projectiles.words[]` を持つ。`fireballs.active` は `Fireballs::activeFireballs` のraw wordで、kind別countがpackedされるため、学習側ではactive slot件数の `activeSlots` / `slots.length` も併用する。handler addressはNSMB symbol table上の `Fireballs::fireballHandler` / `Projectiles::projectileHandler` に対応する。`fireballs.slots[]` は `FireballHandler::spawn/update` と `Fireball::create` の逆アセンブルに基づき、handler pointer + 4 を起点に16 slot、stride `0x8C`、active byte `+0x80`、source kind `+0x81`、state `+0x83`、facing `+0x85`、slot index `+0x86`、pos `+0x10`、prev pos `+0x20`、velocity `+0x30` として読んでいる。source kindは `0=player0`、`1=player1`、`2=piranha_plant`、`3=fire_bro` で、player fireballのownerは `+0x81` から確定できる。新規ログでは `sourceKind`、`kindName`、`ownerSource=slotKind`、`ownerVerified=1` を保存する。active slotでは `stateBytesOffset=0x80` / `stateBytes[]` と `debugWordsOffset=0x40` / `debugWords[]` も保存する。`statelessOwnerCandidate` / `statelessOwnerConfidence` はkind不明projectile向けの位置/速度fallbackで、通常のplayer/enemy fireballではslot kindを優先する。lifetime、当たり判定状態、state/facingの詳細意味は、Fire Mario stage 0実ログでraw値変化と画面表示を照合して検証する。

現時点の既知カテゴリ:

- `player`
- `big_star_actor`
- `big_star_related`
- `big_star_marker`
- `world_item`
- `neutral_item`
- `coin_item`
- `dropped_star_item`
- `item`
- `coin`
- `moving_hazard`
- `hazard`
- `enemy_goomba`
- `enemy_koopa`
- `platform`
- `warp_entrance`
- `item_spawn_effect`
- `camera`
- `stage_scene`
- `stage_fx`
- `stage_actor_manager`
- `stage_controller`
- `stage_layout`
- `mvl_object267`
- `vs_connect`
- `course_select`
- `object`

## Planned Pipeline

- `MELONDS_NSML_AI_PLAY_LOG=<path>` でAI/人間共通の観測ログを出す。
- 人間プレイを収集する場合は、まず `pwsh scripts\run-nsmb-mvl-human-recording.ps1 -NoPacketCapture -Scenario star-chase` のようにscenario名を付けて使う。これはstage 0固定で、デフォルトではclient/Luigi側1窓だけを起動する `single_client_authoritative` 記録になる。MarioとLuigi、敵、アイテム、地形、入力ラベルは同じclient側melonDSインスタンスから同一frameで保存するため、host/client 2窓同期には依存しない。実プレイ用はJITありを既定にする。JITなしは900F測定で約11fpsしか出ないため、再現性確認などの検証目的でのみ `-NoJit` を指定する。通常はmelonDSを閉じると同じPowerShell上で後処理が自動実行され、`recording.json`、dataset、監査JSON、`.jsonl.gz` が生成される。後処理を後で手動実行したい場合だけ `-NoAutoPostprocess` を使う。途中でwrapperが落ちても `client/ai-playlog.jsonl` と `client/ai-observations-v2.jsonl` が残っている場合は、`pwsh scripts\run-nsmb-mvl-recording-postcommands.ps1 -Session <recording-session.json>` で後処理だけ再実行できる。
- RuleAI相手で人間ログを取る場合は、`-OpponentAI rule` を付ける。既定の `HumanSide=client` ではLuigiを人間が操作し、Mario側をRuleAIが動かす。Fire相手を作る場合は `-Scenario fire-ruleai -OpponentAI rule -ForcePlayerPowerups -ForcePlayerPowerup0 2 -ForcePlayerPowerup1 2` のようにし、終了後の `fireball-audit.json` でowner/sourceを確認する。
- MvLは既定では5先取なので、相手を5スター以上にすると開始直後に勝敗条件を超えて終了する。巨大マリオitemのように劣勢時に偏るitemを狙う場合は、`-Scenario mega-item-losing -ForcePlayerStarCounters -ForcePlayerStarCountersStartFrame 900 -ForcePlayerStarCountersEndFrame 1500 -ForcePlayerBattleStars0 4 -ForcePlayerBattleStars1 0 -ForcePlayerDisplayedStars0 4 -ForcePlayerDisplayedStars1 0 -ForcePlayerCollectedStars0 4 -ForcePlayerCollectedStars1 0` を付け、Luigi/P1が0対4で劣勢になる初期条件を作る。Mario/P0を劣勢にする場合は0/1の値を入れ替える。
- `-Scenario` は `recording-session.json` と後処理後の `recording.json.metadata.scenario` に保存される。手動ログは `star-chase`、`item-box`、`fire`、`enemy-hazard`、`free-play` などに分け、後からdataset品質やイベント不足をscenario単位で確認できるようにする。
- `-MvlMatchSeed` 未指定時は、`run-nsmb-mvl-human-recording.ps1` が記録ごとにmatch seedを自動生成し、`recording-session.json` と後処理後の `recording.json.replay.matchSeed` に保存する。同じ配置を再検証したい場合だけ `-MvlMatchSeed 0x00000001` のように明示固定する。
- 2窓host/client記録は `pwsh scripts\run-nsmb-mvl-human-recording.ps1 -DualWindow` で起動できるが、現時点では実験用。peer未接続や入力待ち停止を避けるための調整中で、学習用の人間ログ収集では既定のclient-only権威ログを使う。packet capture / packet replay は2窓完全再現用なので、client-only既定では無効扱いになる。
- 記録終了後は `pwsh scripts\run-nsmb-mvl-recording-postcommands.ps1 -Session <recording-session.json>` で、packet replay変換、manifest/index、dataset生成をまとめて実行する。手順確認だけなら `-DryRun`、途中から再開する場合は `-StartAt <index>` を使う。
- `python scripts\nsmb_mvl_ai_audit_recordings.py <recording.json|recordings-index.json> --stage 0 --min-player-found-ratio 0.5 --min-label-ratio 0.5` で、学習前の最低限の品質を確認する。packet replay完全再現を前提にする人間記録では `--require-packet-replay`、Fire Marioなど低頻度scenarioでは `--require-event fireballActive:1` のようにevent条件を追加する。
- `python scripts\nsmb_mvl_ai_audit_visual_state.py <playlog.jsonl> [<playlog2.jsonl> ...] --output <visual-state-audit.json>` で、`visualState` 欠落、powerup/inventory raw値分布、無敵状態未確定、damage guard/shell観測frame、可視未知object、fireball owner低信頼slotを確認する。client-only人間記録ではclient playlog 1本だけを渡す。2窓検証ではhost/client両方を渡す。人間記録のpostCommandsにはこの監査を含める。学習前ゲートとして止めたい場合は `--strict`、または `--fail-on-visual-state-missing` / `--fail-on-invincibility-unknown` / `--fail-on-visible-unknown-object` / `--fail-on-fireball-low-confidence` を使う。
- `python scripts\nsmb_mvl_ai_audit_fireballs.py <playlog.jsonl> [<playlog2.jsonl> ...] --output <fireball-audit.json>` で、Fireball handler slotのowner/source/state/facing/lifetimeを集計する。旧ログで `kindName` や `ownerVerified` がない場合も、`kind/sourceKind` が `0/1/2/3` なら補正して扱う。
- `python scripts\nsmb_mvl_ai_audit_items.py <playlog.jsonl> [<playlog2.jsonl> ...] --output <item-audit.json> --csv <item-audit-clusters.csv> --svg-dir <svgs> --svg-per-cluster 2` で、item actor種別候補をクラスタリングし、代表SVGを生成する。kind推定は `kindCandidate` と `kindConfidence` で出す。confidenceは `3=状態変化/カテゴリ/手動目視で確定`、`2=ログ上の取得候補として強い`、`1=settingsパターンからの候補`、`0=未分類`。2026-06-14時点では `coin`、`dropped_battle_star`、`fire_flower`、`shell`、`mini_mushroom`、`mega_mushroom`、`invincible_star` は確定があり、`0x00090000` は現在状態依存の `adaptive_powerup` として、通常状態ではスーパー、スーパー状態ではファイアに文脈分類する。`super_mushroom_candidate`、`unknown_item_variant_candidate` は候補として分離する。Blue Shellは `0x0001108B`、マメは `0x00011099`、巨大キノコは `0x00011085`、無敵スターは `0x00011081` を手動目視ログで確認済み。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-item-audit-sweep.ps1 -Policies rule -Seeds "0x2f52869f,0x12345678" -StarCases "even:0:0,p0-losing-hard:0:4,p1-losing-hard:4:0" -Frames 2600 -AllowJit` で、seedとBattle Star差を振ってitem出現を収集し、各runに `item-audit.json` / `item-audit-clusters.csv` / `item-audit-svgs` を作る。まず起動計画だけ確認する場合は `-DryRun` を付ける。デフォルトのStarCasesも巨大マリオ確認を優先して5先取/4点差にしている。
- `python scripts\nsmb_mvl_ai_audit_dataset_terrain.py <ai-dataset-player1.csv> --output <dataset-terrain-audit.json>` で、学習用terrain列がfoot側blockだけで横壁になっていないか、旧raw summaryから何件補正されたか、隠し/救済/中身入りblock候補がどれだけ出ているかを確認する。これは従来CSV向けの互換/監査ツールなので、人間記録の通常postCommandsには含めず、`-BuildLegacyDataset` 指定時だけ実行する。
- `python scripts\nsmb_mvl_ai_create_recording_manifest.py <playlog.jsonl> <recording.json> --kind human --player <0|1> --label-source player --stage 0` で、1本の人間/AIログを記録manifest化する。
- `python scripts\nsmb_mvl_ai_make_recordings_index.py <recordings-index.json> <recording1.json> <recording2.json> ... --stage 0` で、複数記録を1つの学習入力に束ねる。
- `python scripts\nsmb_mvl_ai_build_dataset.py <playlog.jsonl> <dataset.csv> --player 1 --require-player-found` で固定長特徴量へ変換する。デフォルトの `--label-source auto` は `appliedPlayerN` があればそれを使い、なければ `playerN` を使う。人間ログだけを明示する場合は `--label-source player` を指定する。
- `python scripts\nsmb_mvl_ai_build_dataset.py <recording.json|recordings-index.json> <dataset.csv> --player 1 --label-source player --require-player-found` でも固定長特徴量へ変換できる。
- datasetには `nearest_<category>_*` の単純最近傍に加えて、接近中/至近性で重み付けした複合危険対象 `runtime_hazard_*` も入る。Goomba/Koopa/シェルなどへの回避を学習させる場合は、`runtime_hazard_closing`、`runtime_hazard_very_close`、`runtime_hazard_dx/dy/vx/vy` を優先的に見る。
- 8コイン成立直後の降下itemは、`self_prev_coins`、`self_coin_reward_recent`、`self_coin_reward_age`、`coin_reward_item_*` として明示列にする。最近傍item全般は `nearest_item_*` に入り、objectId/settings/settings_low8/vtable/velocity/screen座標/powerup候補/種別候補/避ける候補を保持する。item種別は `*_kind_candidate` と `*_kind_confidence` を主列にし、補助フラグとして `*_is_super_mushroom_candidate`、`*_is_fire_flower_candidate`、`*_is_coin_item_candidate`、`*_is_dropped_star_candidate`、`*_is_mini_mushroom_candidate`、`*_is_shell_candidate`、`*_is_mega_mushroom_candidate`、`*_is_invincible_star_candidate`、`*_is_unknown_item_variant_candidate` を出す。マメ候補が確定した場合だけ `*_avoid_candidate` を立て、raw settings/vtable/objectIdは再分類用に必ず残す。
- `python scripts\nsmb_mvl_ai_train_imitation.py <dataset.csv> <model.npz>` でキー入力の多ラベル分類モデルを学習する。複数記録を使う場合は `--split-by-recording` で記録単位validationにする。
- `python scripts\nsmb_mvl_ai_predict_imitation.py <model.npz> <dataset.csv> <predictions.csv>` で学習済みモデルのオフライン推論結果を確認する。
- `python scripts\nsmb_mvl_ai_export_runtime_model.py <model.npz> <runtime-model.json>` で、C++ランタイム用の線形ポリシーモデルへ変換する。
- `build\release-windows-x86_64\nsmb_mvl_imitation_cpp_predict.exe <runtime-model.json> <dataset.csv> <cpp-predictions.csv>` で、C++ランタイムによるdataset CSV推論を実行する。
- `python scripts\nsmb_mvl_ai_compare_cpp_imitation.py <model.npz> <dataset.csv> <runtime-model.json> <cpp-exe> <output-dir>` で、Python推論とC++推論の `pred_held` / button確率をフレーム単位で比較する。
- melonDS実行時の逐次推論は、runtime JSONを絶対パスで渡して使う。例: `MELONDS_NSML_IMITATION_AI=1`、`MELONDS_NSML_IMITATION_AI_MODEL=C:\...\human-stage0-player1-runtime-model.json`、`MELONDS_NSML_IMITATION_AI_PLAYER=LUIGI`、`MELONDS_NSML_IMITATION_AI_THRESHOLD=0.5`。`MELONDS_NSML_IMITATION_AI_TRACE=1` と `MELONDS_NSML_IMITATION_AI_TRACE_INTERVAL=120` で、feature coverage、held、button確率をstdoutへ出せる。2窓同期で同じキャラを動かす場合は `LUIGI` / `MARIO` / `1` / `0` のようなlogical player指定を使う。`remote` はhostではLuigi、clientではMarioを指すため、両窓に同じ環境変数を渡す2窓実行では同期確認に使わない。
- 実行時推論は危険な未使用ボタンを避けるため、デフォルトで `MELONDS_NSML_IMITATION_AI_ALLOWED_HELD_MASK=0x8F3` 相当、つまり A/B/Right/Left/Up/Down/Y だけを通す。左右同時、上下同時は確率が高い側だけ残す。必要ならenvでmaskを変えられる。
- 閉ループ評価は `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-ai-closed-loop-eval.ps1 -Policy imitation -Frames 2600 -AllowJit` で実行する。`-Policy neutral` / `-Policy rule` へ切り替えると同条件baselineを作れる。結果は `<LogDir>\closed-loop-eval.json`。`-Policy rule` では `<LogDir>\host\ruleai-audit.json` と `<LogDir>\client\ruleai-audit.json`、`-Policy imitation` では `<LogDir>\host\imitation-audit.json` と `<LogDir>\client\imitation-audit.json` も自動生成される。既存ログだけを評価する場合は `python scripts\nsmb_mvl_ai_evaluate_closed_loop.py <ai-playlog.jsonl|game-state.csv> ... --player 1 --policy <name> --output <closed-loop-eval.json>` を使う。ImitationAIのneutral/stuckだけを既存ログで見る場合は `python scripts\nsmb_mvl_ai_audit_imitation.py <ai-playlog.jsonl[.gz]> --player 1 --output <imitation-audit.json>` を使う。
- 複数の閉ループ評価を比較する場合は `python scripts\nsmb_mvl_ai_compare_closed_loop.py <eval1.json> <eval2.json> ... --output-json <compare.json> --output-csv <compare.csv>` を使う。host/client両方の評価行を残すため、同期差や入力反映差も確認できる。
- `python scripts\nsmb_mvl_ai_predict_input_script.py <model.npz> <playlog.jsonl|recording.json|recordings-index.json> <model.inputs> --player 1 --prefix-script tests\nsmb_us_direct_mvl_minimal_bootstrap.inputs --end-frame <frame>` で、学習済みモデルの予測をmelonDS input scriptへ戻す。`--reaction-delay-frames`、`--mistake-rate`、`--mistake-mode` で実プレイ向けの強さ調整候補も入れられる。
- `python scripts\nsmb_mvl_ai_verify_replay.py <expected recording.json|playlog.jsonl> <actual playlog.jsonl>` で、melonDS replayが完全再現できているかを最終状態で検証する。
- `pwsh scripts\run-nsmb-mvl-recording-replay.ps1 -RecordingManifest <recording.json>` で、manifest内の `replay.mode` に従ってinput script replayまたはpacket replayを起動し、host/client別AI play logを取り直して検証する。起動計画だけ確認する場合は `-DryRun` を使う。最初のズレを調べる場合は `-ScanFrames`、間引き確認は `-CheckpointInterval 30 -CheckpointStartFrame 900` などを指定する。
- `python scripts\nsmb_mvl_ai_export_viewer_data.py <playlog.jsonl> <viewer-data.json>` で、GUI/外部ビューア向けJSONを作る。GUIの `AIログ` タブはJSONLを直接読むこともできる。
- `python scripts\nsmb_mvl_ai_inspect_playlog.py <playlog.jsonl> --player 1` で、frame、入力、接地/壁/水などのcontact、player/相手/星/hazardの相対位置、可視X数、カテゴリ数を目視確認する。
- `python scripts\nsmb_mvl_ai_render_playlog_svg.py <playlog.jsonl> <frame.svg> --player 1 --frame <frame>` で、player中心の状態確認SVGを生成する。SVGにはplayer/相手、入力、powerup/inventory、接地/壁/穴summary、fireball owner、gameplay object、近接tileProbe sampleに加えて、`tileProbe.grid` の広域地形を `?=question/item block`、`B=breakable block`、`R=brick`、`H=hidden/invisible`、`C=coin`、`!=harmful`、`E=entrance/pipe` などの面表示で重ねる。stage管理用のcamera/controller/layout objectと、座標が実スターではない `big_star_related` 補助actorは描画から外し、未解決tileはメイン図には塗らずヘッダの `unresolved` countで確認する。stage wrapはplayer中心の最短距離へ正規化し、NSMBのworld Y符号を表示向けに反転して描く。
- `python scripts\nsmb_mvl_ai_render_eval_events.py <closed-loop-eval.json> --output-dir <dir> --events deathTransitions,starPickups --offsets=-30,0,30 --player 1` で、閉ループ評価のeventSamplesから死亡/スター取得の前後frameをまとめてSVG化する。複数の `closed-loop-eval.json` を渡せるので、DAgger前処理の失敗frame確認に使う。
- `python scripts\nsmb_mvl_ai_catalog_objects.py <playlog.jsonl>` で、未知objectの出現頻度と代表的な相対位置を確認し、カテゴリ付けを増やす。
- `python scripts\nsmb_mvl_ai_catalog_tiles.py <playlog.jsonl> --player 1` で、StageLayout tileProbeのtile id / behavior / lowType / status / contact ground / effective groundを集計し、床/穴/箱/未取得tileの候補を調べる。
- その後、同じ観測schemaを使って自己対戦学習へ進む。
- 強さ調整は、推論時に入力反応遅延、ランダムミス、action hold制限、近傍探索幅制限を入れる。

## Imitation Learning Readiness Plan

模倣学習へ進む前の基準は、「人間がstage 0の画面を見て入力判断に使う情報」と「その時点の入力ラベル」が、同じframe番号で再現・目視検証・dataset化できること。学習モデルの高度化より先に、観測、replay、データ品質、推論戻し経路を固める。

1. 状態取得を目視同等へ近づける。
   - まずstage 0固定で、player、相手、Big Star、coin、item、enemy、hazard、platform、block/item box、camera、地形接触、落下危険、入力を1フレーム単位で揃える。
   - item boxは「叩く前の中身候補」「叩いた瞬間」「使用済みtileへの変化」「spawnしたitem/effect」を同じtimeline上で追えるようにする。
   - Fire Marioのfireball、敵/ステージ由来projectile、短命effectは object ID/vtable/settings/owner候補/速度/寿命をログへ出し、`projectile` / `player_fireball` / `enemy_projectile` のカテゴリへ分ける。
   - powerup変化、ダメージ、死亡、スター取得、コイン取得、item取得、ブロック破壊、敵撃破、落下復帰などをevent候補としてmanifest/viewerへ出す。
   - powerup/inventory raw値、damage guard timer/cooldown/flag、physics guard、shell stateは `visualState` へ出るようになった。画面表示との照合で `mappingVerified=1` にできるまで、Fire/Shell/Megaなどは候補扱いにし、スター無敵/ダメージ後無敵/powerup適用中無敵の原因分類は実ログで詰める。
   - `audit_visual_state` の結果で `visualStateMissing`、可視未知object、fireball owner低信頼slot、invincibility unknown、damage guard/shell観測frameを毎回確認し、目視同等に足りない状態を次の解析対象にする。
   - `tileId=0x001 behavior=0x0000002A lowType=42` の低位tile意味をstage 0実ログと逆アセンブルで潰し、raw `solidish` / `hole*` を接地contact補正なしでもなるべく正しくする。`tileId=0x110-0x113` は動的behavior table参照先修正で取得できるようになった。

2. 人間プレイ記録を完全再現できるreplay基盤にする。
   - 記録manifestには、ROM/build識別子、stage、seed、host/client role、local player、入力scriptまたはpacket replay、AI play log、viewer data、dataset、検証結果を保存する。
   - `recording.json` からmelonDSをreplay起動し、終了後に `nsmb_mvl_ai_verify_replay.py` まで自動実行する。専用ラッパーはinput script replayとpacket replayの起動計画を扱えるようになったので、次は実際の人間記録で完全一致を確認する。
   - 検証は最終frameだけでなく、checkpoint frameのhash、player座標/powerup/dead/star/coin、object category count、event列を比較できるように拡張する。
   - replayが完全一致しない場合でも、どのframeからズレたかをviewerとCSVで追えるようにする。現時点では `--scan-frames` と mismatch JSON/CSV reportで最初のズレframe、player状態、hash、object/category count差分を保存できる。

3. 外部ビューア/GUIでデータ品質を人間が確認できるようにする。
   - 現在のSVG相当表示に加えて、timeline、入力列、event列、object category filter、unknown object一覧、tileProbe/block状態、replay差分を表示する。
   - 「このフレームの人間入力がなぜ妥当か」を確認できるように、player中心相対配置、カメラ内可視状態、足元/前方地形、item/projectileの寿命変化を同時に見る。
   - 目視検査済み/破棄/要再分類などの品質タグをmanifestまたはindexへ残す。

4. dataset作成を人間ログ向けに強くする。
   - 低頻度イベントが消えないよう、通常走行、スター争奪、item取得、Fire Mario、敵/穴/ブロック接触、死亡前後をscenarioタグで分けて収集する。
   - `--split-by-recording` を標準にし、同じ連続プレイの隣接frameがtrain/validationに混ざらないようにする。
   - button別accuracyだけでなく、held完全一致、方向入力、jump/fire同時押し、イベント周辺window、危険回避windowの評価を出す。
   - 人間ログ、RuleAIログ、将来の自己対戦ログを混ぜられるよう、source/kind/player/role/stage/scenario/qualityを特徴量ではなくmetadataとして保持する。

5. 学習モデルをゲーム入力へ戻して閉ループ評価する。
   - 既存 `.npz` の簡易モデルは、C++ランタイムでdataset CSVに対してPython推論と一致し、さらにmelonDS実行時に `GameStateSample` / object scan / fireball handler状態から同じfeature vectorを組み立て、input script生成を介さずremote CPU入力へ戻すところまで確認済み。
   - 次は閉ループ実走品質評価として、stage 0で一定時間生存、Big Starへ近づく、落下しない、相手と戦う、itemを使う、というゲーム内指標をmanifestへ入れる。
   - 推論時は直近数frameの状態、前回入力、action hold、反応遅延、ランダムミス率、無効入力抑制を入れる。
   - オフライン一致率だけでなく、stage 0で一定時間生存、Big Starへ近づく、落下しない、相手と戦う、itemを使う、というゲーム内指標で評価する。

6. その後に自己対戦へ進む。
   - 模倣学習済みモデルを初期値にし、RuleAIを相手/補助データ生成器として残す。
   - 報酬は勝敗だけにせず、スター差、死亡、相手/星への距離、item取得、危険回避などstage 0で観測済みのイベントを使う。
   - 学習が破綻した場合に人間ログへ戻せるよう、自己対戦ログも同じmanifest/viewer/dataset形式で保存する。

## Current Blockers / Unknowns

- object IDと画面上の意味の対応はまだ完全ではない。coin/item/enemy/platform/hazard の初期カテゴリは入り、block/item box候補はStageLayout tileProbeから取れるようになったが、stage 0の実ログを見ながらステージ固有ギミックの分類を増やす。
- 目視同等にするには、stage 0の実プレイ場面でタイルサンプルを増やし、穴/壁/床判定のoffsetとsolid maskを調整する必要がある。左右ラップ込みX判定、Y込みの完全可視判定、player接触地形、CollisionMgr接触結果、modifier tile、tile damage、playerの画面Y/カメラ底距離、StageLayout由来の前方/左右タイルサンプルは取れるようになった。
- 17点raw probeでは、stage 0の接地中に `tileId=0x001 behavior=0x0000002A lowType=42` が出る場面があり、点probe単体のsolidishでは床として分類できないことがある。学習用/RuleAI用の `groundBelowSolid` と `effectiveHole*` はterrain grid由来の床row推定へ移したため、この問題は短縮scalarには直接入らない。raw probeの低位tile typeは監査情報として残し、旧ログで大量に出ていた `tileId=0x110-0x113 status=6` はdynamic behavior table pointer修正後の新規ログでは消えている。
- ブロック/アイテム箱の「中身」はblock flagつきStageLayout tile behaviorのstorage contentsとして保存するようになった。叩いた後の状態は現在tile id/behaviorの変化として取れる想定だが、stage 0の実ログで `StageLayout::changeTile` / question block animation path と照合して詰める。
- powerup / inventory powerupは raw値、actor内のpowerup/form state、shell stateを統合した暫定 `visualState` としてログ/dataset/SVG/manifestへ流れるようになった。`mappingVerified=0` のままなので、Fire/Shell/Mega候補は学習特徴として使えるが、最終的な意味付けはまだstage 0実ログで画面表示と照合する必要がある。
- `logs/nsmb-mvl-human-recording-stage0-20260608-012640` の人間ログでは、Luigiがファイアーボールを撃つ場面でも `players[1].powerup` は0固定、`inventoryPowerup` は1固定だった。一方で `players[1].visualState.actorPowerupState` / `actorPowerupFormState` は0/1/2に変化し、最終的に2が長く出る。新規ログでは `visualPowerupKindCandidate` と `canShootFireVisualCandidate` を併用し、古い同ログは新フィールド追加前の記録として扱う。
- スター無敵、ダメージ後無敵、巨大/豆/甲羅中の一時状態について、damage guard timer/cooldown/flag、physics guard、shell stateはログ/dataset/SVG/manifestへ流れるようになった。`visualState.invincibleKnown=1` と `audit_visual_state.invincibilityUnknown=0` は確認済み。ただし、どのvisual effectがスター由来か、ダメージ後由来か、powerup適用中由来かの原因分類は未確定で、stage 0実ログで画面表示とtimer変化を照合する必要がある。
- 8コイン報酬itemはdatasetに明示列が入ったが、item settingsと画面上の種類の対応はまだ完全ではない。`0x0001108B` は当初マメ系と疑っていたが、`logs\nsmb-mvl-human-recording-stage0-20260613-232727\client\ai-playlog.jsonl` frame 32280 の手動目視で甲羅itemと確認したため、`shell_candidate` へ修正済み。raw settingsを列に残しているため、mappingだけ後から修正してdatasetを再生成できる。
- 自己対戦に進む前に、ログschemaを実プレイログで増強し、学習済みモデルを入力へ戻した状態でstage 0を閉ループ評価する必要がある。現時点では `.npz` からmelonDS input scriptを生成する経路、同じ線形モデルをC++で推論してPython推論と一致させる経路、PoC内の実RAM状態からdatasetと同一feature vectorをC++で組み立てて逐次推論結果を即時入力へ戻す経路、閉ループ評価JSONを作る経路ができた。未完了なのは、長めの実走でneutral/RuleAI/ImitationAIを比較して実戦品質を測ることと、現行線形モデルより強いモデル/データ量への改善。
- 2026-06-14時点のRuleAIは、人間ログ収集の既定相手であるMario側では `logs/codex-ruleai-mario-only-recovery-20260614` の3 seed x host/clientで `avgDeathTransitions=0.3333333333`、`avgAliveRatio=0.9346224678`、`avgStarPickups=0.1666666667`。Luigi側回帰確認 `logs/codex-ruleai-luigi-mario-gated-regression-20260614` は `avgDeathTransitions=0.1666666667`、`avgAliveRatio=0.9484346225`、`avgStarPickups=0.5`。ただしMario側 `seed=0x13579bdf` hostのGoomba接触死と、`seed=0x12345678` clientのremote表示上の穴落ちは残る。RuleAIは人間ログ前の相手/分布拡張器として使える段階に近づいたが、まだ「強いAI」ではない。
- ImitationAIはobject scan全体のhazard threat化後も5seed平均では `avgStarPickups=0.0`、`avgDeathTransitions=1.0`、`avgAliveRatio=0.8833333333`、`avgBigStarApproachDelta=-2189652024524.8`。単純な「危険方向を消す/逃げ方向へ入れる」だけでは、壁際・着地・敵/hazard同時接近を解けないため、次は失敗frameをviewer/SVGで見ながら人間ラベル追加またはDAgger対象にする。
- 性能/同期面では、2窓 input-netplay の手動記録はまだ学習ログ既定にできない。`logs/nsmb-mvl-human-recording-stage0-20260608-005445` と同期再試行ログではpeer接続が成立せず、host/clientのgameplay heartbeat状態も一致しなかった。`run-nsmb-mvl-netplay-route-smoke.ps1` ではpeer接続自体は成立するため、問題は手動2窓記録時の input-netplay/defer/start wait 経路にある。学習用の人間記録は、修正済みの `single_client_authoritative` で取る。
- 学習・大量評価には、client-only記録だけでなく、PoC内直結推論、単一プロセス評価、ヘッドレス高速実行、または状態遷移だけを回す専用runnerが必要。client-only記録は実プレイログ収集には足りるが、自己対戦を高速に回すにはまだ専用化が必要。
- fireballなどのprojectile系objectは、通常actor object listではなく専用handlerで管理されるものがある。Fireball slot別の座標/前フレーム座標/速度/source kind/state/facing、player fireball ownerは取れるようになった。`Fireball::create` の逆アセンブルにより、slot `+0x81` がsource kindで、player fireballでは `0/1` がowner player idであると確定した。残課題は寿命、当たり判定状態、state/facingの詳細意味、敵fireball kind `2/3` の画面上挙動の照合。
- `run-nsmb-mvl-recording-replay.ps1` はinput script replayとpacket replayの起動計画を扱え、`-ScanFrames` で不一致reportも残せる。ただしclient-only人間記録の完全replay検証と、2窓packet replay完全再現はまだ未完了。2窓packet replayはDualWindow記録が安定してから再開する。

## Verification

- 2026-06-09 C++模倣推論ランタイム追加後に `python -m py_compile scripts\nsmb_mvl_ai_export_runtime_model.py scripts\nsmb_mvl_ai_compare_cpp_imitation.py scripts\nsmb_mvl_ai_predict_imitation.py` pass。
- 2026-06-09 C++模倣推論ランタイム追加後に `g++ -std=c++17 -O2 -Isrc/frontend/qt_sdl tools/nsmb_mvl_imitation_cpp_predict.cpp src/frontend/qt_sdl/NsmbImitationAI.cpp -o tmp/nsmb_mvl_imitation_cpp_predict.exe` pass。
- 2026-06-09 C++模倣推論ランタイム追加後に `cmake --build build\release-windows-x86_64 --target nsmb_mvl_imitation_cpp_predict --config Release` pass。
- 2026-06-09 C++模倣推論ランタイム追加後に `cmake --build build\release-windows-x86_64 --target melonDS --config Release` pass。既知の `clang++: argument unused during compilation: '-s'` と `lld-link: found both wWinMain and WinMain; using latter` 警告のみ。
- 2026-06-09 `logs\codex-human-imitation-baseline-20260608\human-stage0-player1-imitation.npz` を `logs\codex-cpp-imitation-parity-20260609\human-stage0-player1-runtime-model.json` へexportし、`logs\codex-human-imitation-baseline-20260608\human-stage0-player1-dataset.csv` 36,281行でPython推論とC++推論を比較。`build\release-windows-x86_64\nsmb_mvl_imitation_cpp_predict.exe` で `button_acc=0.951 exact=0.569`、Python側と同一、`pred_held` 全行一致、最大確率差 `0.00000100`。
- 2026-06-09 実行時ImitationAI入力戻し追加後に `cmake --build build\release-windows-x86_64 --target melonDS nsmb_mvl_imitation_cpp_predict --config Release` pass。
- 2026-06-09 実行時ImitationAI入力戻し追加後に、同じ36,281行datasetで `python scripts\nsmb_mvl_ai_compare_cpp_imitation.py ... logs\codex-cpp-imitation-parity-20260609\after-runtime-input-hook --threshold 0.5` pass。`pred_held` 全行一致、最大確率差 `0.00000100`。
- 2026-06-13 `runtime_hazard_*` dataset特徴追加後に `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py` pass、`cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。既知の `clang++: argument unused during compilation: '-s'` と `lld-link: found both wWinMain and WinMain; using latter` 警告のみ。
- 2026-06-13 `logs\codex-ruleai-hazardclose-seed13579bdf-2400-20260613\host\ai-playlog.jsonl` から `logs\codex-runtime-hazard-dataset-check-20260613.csv` を生成し、死亡直前frame 2070-2160で `runtime_hazard_found=1`、`runtime_hazard_closing=1`、`runtime_hazard_very_close=1`、`runtime_hazard_object_id=83` が出ることを確認。Goomba/動くhazard接触死を学習特徴量として説明できる。
- 2026-06-13 新feature 1738列の小モデル `logs\codex-runtime-hazard-model-check-20260613\runtime-model.json` をexportし、`logs\codex-runtime-hazard-cpp-parity-20260613` でPython/C++推論比較pass。51行で `pred_held` 一致、最大確率差 `0.00000100`。同モデルを `logs\codex-runtime-hazard-imitation-coverage-1600-20260613` でmelonDSへ読み込ませ、1600F閉ループsmoke pass、missing feature警告なし。
- 2026-06-13 `hazardJump` RuleAI試作は `logs\codex-ruleai-hazardjump-route-2seed-2600-20260613` で不採用判定。seed `0x13579bdf` のhost/client死亡が戻り、seed `0x12345678` hostのBig Star接近も改善しなかったため、コードから戻した。
- 2026-06-13 fireball専用監査追加後に `python -m py_compile scripts\nsmb_mvl_ai_audit_fireballs.py` pass。`logs\human-stage0-star-chase-004\client\ai-playlog.jsonl` と `005` を `logs\codex-fireball-audit-004-005-20260613.json` へ監査し、8319行中fireballあり164行、active slot sample 190、event 11、sourceKinds `1` / kindNames `player1` / ownerCandidates `1` が190件、owner verified 190件、low confidence 0件を確認。既存Fire MarioログではLuigi側fireball owner/sourceは学習特徴量として使える。
- 2026-06-13 `run-nsmb-mvl-human-recording.ps1` のpostCommandsへfireball auditを追加後、`logs\codex-human-recording-fireball-audit-dryrun-20260613` で `-Scenario fire -NoPacketCapture -DryRun` pass。`recording-session.json` に `fireballAudit` と `scripts\nsmb_mvl_ai_audit_fireballs.py ... --output <fireball-audit.json>` が入ることを確認。
- 2026-06-13 RuleAI相手の人間記録導線追加後に PowerShell Parser pass。`logs\codex-human-recording-ruleai-fire-dryrun-20260613` で `run-nsmb-mvl-human-recording.ps1 -DryRun -NoPacketCapture -Frames 120 -Scenario fire-ruleai -OpponentAI rule -ForcePlayerPowerups -ForcePlayerPowerup0 2 -ForcePlayerPowerup1 2` を実行し、`recording-session.json` に `opponentAI=rule`、`opponentAIPlayer=MARIO`、Fire開始引数、通常のmanifest/index/dataset/visual audit/fireball audit postCommandsが入ることを確認。
- 2026-06-09 `logs\codex-imitation-runtime-smoke-final-20260609`: `MELONDS_NSML_IMITATION_AI=1`、`MELONDS_NSML_IMITATION_AI_MODEL=<absolute runtime json>`、`MELONDS_NSML_IMITATION_AI_PLAYER=remote`、`MELONDS_NSML_IMITATION_AI_TRACE=1` でsplit local-input smoke 1100F pass。stdoutで model load、`features=1727/1727`、frame 960/1080 の推論trace、remote wait 0を確認。active fpsはhost 59.95、client 59.75。これは接続smokeであり、2窓同期確認やAIの強さ評価ではない。
- 2026-06-09 `logs\codex-imitation-luigi-sync-compare-20260609-004431`: `MELONDS_NSML_IMITATION_AI_PLAYER=LUIGI` でsplit local-input smoke 1100F pass。host/client双方でlogical `player=1` を推論し、game-state comparisonありで通過。2窓で同じLuigiをAI操作する確認では `remote` ではなく `LUIGI` を使う。
- 2026-06-09 `scripts\nsmb_mvl_ai_evaluate_closed_loop.py` py_compile pass。`logs\codex-ai-eval-playlog-probe-20260609-200925` のhost/client AI play logと、`logs\codex-imitation-luigi-sync-compare-20260609-004431` のhost/client game-state CSVを評価し、`closed-loop-eval.json` 生成pass。
- 2026-06-09 `scripts\run-nsmb-mvl-ai-closed-loop-eval.ps1`: `-Policy neutral` / `-Policy rule` / `-Policy imitation` の短時間950F smoke pass。`-Policy imitation -Frames 1100 -AllowJit` はgame-state comparisonありでpassし、`logs\codex-ai-closed-loop-imitation-compare-20260609\closed-loop-eval.json` を生成した。host/clientとも `player=1`、`rows=10`、`deathTransitions=0`、`bigStarDistance.approachDelta=5357681057791`。
- 2026-06-09 2600F閉ループ初期比較: `logs\codex-ai-closed-loop-neutral-2600-20260609`、`logs\codex-ai-closed-loop-rule-2600-20260609`、`logs\codex-ai-closed-loop-imitation-2600b-20260609` を `scripts\nsmb_mvl_ai_compare_closed_loop.py` で比較し、`logs\codex-ai-closed-loop-compare-2600-20260609.json/csv` を生成した。同seedではImitationAIのみ `starPickups=1` / `starDiffEnd=1` / `bigStarApproachDelta=2359010787328`。neutralはstar 0、RuleAIもstar 0。全policyで `deathTransitions=1` が残り、次の改善対象は落下/死亡回避。
- 2026-06-09 `logs\codex-ai-closed-loop-imitation-2600b-20260609\closed-loop-eval.json` をevent sample対応で再生成し、ImitationAIの死亡frameは1470、star pickup frameは1860と特定。`frame-1440-before-death.svg`、`frame-1470-death.svg`、`frame-1500-after-death.svg` を生成した。
- 2026-06-09 neutral 2600Fをgame-state comparisonありで走らせたところ、評価JSON生成後にframe 2190の `movingHazardX` がhost/clientでズレた。長時間のAI品質評価では一旦 `-SkipGameStateComparison` を使い、同期検査は別枠で継続する。
- 2026-06-09 ImitationAI hazard guard追加後に `cmake --build build\release-windows-x86_64 --target melonDS --config Release` pass。既知の `clang++: argument unused during compilation: '-s'` と `lld-link: found both wWinMain and WinMain; using latter` 警告のみ。
- 2026-06-09 hazard guard評価: `logs\codex-ai-closed-loop-imitation-hazardguard-2600-20260609` は縦範囲が狭く、star pickupは2回に増えたがdeath transitionも2回へ悪化。`logs\codex-ai-closed-loop-imitation-hazardguard-wide-2600-20260609` で縦範囲を広げるとdeath transitionは1回に戻り、coin changeが1件増えた。壁詰まり時の水平入力抑制後の `logs\codex-ai-closed-loop-imitation-hazardguard-wall-2600-20260609` でもdeath transitionは1回、star pickupは1回、coin changeは1回で、元の1470死亡は未解決。比較は `logs\codex-ai-closed-loop-compare-hazardguard-wall-2600-20260609.json/csv`。
- 2026-06-09 閉ループbatch追加後に、PowerShell Parserで `scripts\run-nsmb-mvl-ai-closed-loop-eval.ps1`、`scripts\run-nsmb-mvl-ai-closed-loop-batch.ps1`、`scripts\run-nsmb-mvl-split-local-input-smoke.ps1` の構文確認pass。`run-nsmb-mvl-split-local-input-smoke.ps1` は絶対 `-LogDir` を扱えるように修正した。
- 2026-06-09 `logs\codex-ai-closed-loop-batch-smoke-20260609`: `run-nsmb-mvl-ai-closed-loop-batch.ps1 -Policies neutral,imitation -Seeds 0x2f52869f -Frames 950 -AllowJit -SkipGameStateComparison` pass。`closed-loop-batch.json` と `closed-loop-compare.json/csv` を生成。
- 2026-06-09 `logs\codex-ai-closed-loop-batch-2seed-smoke-20260609`: seed正規化修正後に `-Policies neutral -Seeds "0x2f52869f,0x13579bdf" -Frames 950` pass。2つのseedディレクトリが分かれて生成されることを確認。
- 2026-06-09 `logs\codex-ai-closed-loop-batch-2seed-2600b-20260609`: `-Policies neutral,rule,imitation -Seeds "0x2f52869f,0x13579bdf" -Frames 2600 -AllowJit -SkipGameStateComparison` pass。aggregateは ImitationAI `avgStarPickups=0.5` / `avgDeathTransitions=1.0`、neutral `avgStarPickups=0.0` / `avgDeathTransitions=1.0`、RuleAI `avgStarPickups=0.0` / `avgDeathTransitions=1.5`。
- 2026-06-09 RuleAI hazard mode追加後に `cmake --build build\release-windows-x86_64 --target melonDS --config Release` pass。既知の `clang++: argument unused during compilation: '-s'` と `lld-link: found both wWinMain and WinMain; using latter` 警告のみ。
- 2026-06-09 `logs\codex-ai-closed-loop-rule-hazard-2seed-2600-20260609`: RuleAIのみ `-Seeds "0x2f52869f,0x13579bdf" -Frames 2600` で評価pass。`0x2f52869f` はdeath 0、`0x13579bdf` はdeath 3。aggregateは `avgAliveRatio=0.8666666667`、`avgDeathTransitions=1.5`、`avgBigStarApproachDelta=120975137518`。旧2seed batchのRuleAIは `avgAliveRatio=0.85`、`avgDeathTransitions=1.5`、`avgBigStarApproachDelta=-1244960340608`。
- 2026-06-09 `logs\codex-ai-closed-loop-threat-contact-2seed-2600-20260609`: RuleAI/ImitationAIを `-Seeds "0x2f52869f,0x13579bdf" -Frames 2600 -AllowJit -SkipGameStateComparison` で評価pass。RuleAIはobject scan全体からplayer別hazard threatを選び、contact壁も内部壁判定へ反映する変更後、aggregate `avgStarPickups=0.5`、`avgDeathTransitions=1.0`、`avgAliveRatio=0.8833333333`、`avgBigStarApproachDelta=2279574018048`。ImitationAIは `avgDeathTransitions=1.0` のままだが、`0x2f52869f` の旧1470死亡は2310へ変化し、hazard選択の改善は一部効いている。追加で試した `logs\codex-ai-closed-loop-wallrecover-2seed-2600-20260609` の空中壁復帰guardは平均aliveを改善しなかったため不採用。
- 2026-06-09 `logs\codex-ai-closed-loop-threat-contact-5seed-2600-20260609`: RuleAI/ImitationAIを `-Seeds "0x2f52869f,0x13579bdf,0x2468ace0,0x0badc0de,0x31415926" -Frames 2600 -AllowJit -SkipGameStateComparison` で評価pass。aggregateは RuleAI `avgStarPickups=0.2` / `avgDeathTransitions=1.0` / `avgAliveRatio=0.8833333333` / `avgBigStarApproachDelta=1597239840230.0`、ImitationAI `avgStarPickups=0.0` / `avgDeathTransitions=1.0` / `avgAliveRatio=0.8833333333` / `avgBigStarApproachDelta=-2189652024524.8`。5seedではRuleAIが接近指標で優位、現行ImitationAIはDAgger/追加ラベルなしでは伸びない。
- 2026-06-09 `scripts\nsmb_mvl_ai_render_eval_events.py` 追加後に `python -m py_compile scripts\nsmb_mvl_ai_render_eval_events.py` pass。`logs\codex-ai-closed-loop-threat-contact-5seed-2600-20260609\seed-0x2f52869f\rule\closed-loop-eval.json` と `seed-0x13579bdf\imitation\closed-loop-eval.json` から `--events deathTransitions,starPickups --offsets=-30,0,30` で18枚のSVGと `eval-event-svgs.json` を生成できることを確認。
- 2026-06-08 visual powerup候補とfireball owner追跡追加後に `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_create_recording_manifest.py scripts\nsmb_mvl_ai_audit_visual_state.py scripts\nsmb_mvl_ai_render_playlog_svg.py` pass。
- 2026-06-08 visual powerup候補とfireball owner追跡追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。既知の `clang++: argument unused during compilation: '-s'` と `lld-link: found both wWinMain and WinMain; using latter` 警告のみ。
- 2026-06-08 fireball owner解析: `roms\nsmb-us.nds` のoverlay10を逆アセンブルし、`Fireballs::spawnPlayerFireball` がplayer idをkindとして `Fireballs::spawn` へ渡し、`Fireball::create` がkindをslot `+0x81` へ保存することを確認。`FireballHandler::spawn` がslot indexを第5引数として渡し、slot `+0x86` へ保存するため、`+0x86` はownerではなくslot index。
- 2026-06-08 fireball owner verified化後に `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_audit_visual_state.py scripts\nsmb_mvl_ai_render_playlog_svg.py` pass。
- 2026-06-08 fireball owner verified化後に `python scripts\nsmb_mvl_ai_audit_visual_state.py logs\human-stage0-star-chase-004\client\ai-playlog.jsonl logs\human-stage0-star-chase-005\client\ai-playlog.jsonl --output logs\codex-fireball-owner-verified-audit-20260608.json` pass。004はfireball slot 98件、005は92件で、どちらも `fireballOwnerLowConfidence=0`、`fireballStatelessOwnerLowConfidence=0`。
- 2026-06-08 fireball owner verified化後に `python scripts\nsmb_mvl_ai_build_dataset.py logs\human-stage0-star-chase-005\client\ai-playlog.jsonl logs\codex-fireball-owner-verified-dataset-20260608.csv --player 1 --label-source player --require-player-found` pass。fireball行66件すべて `nearest_fireball_kind=1`、`nearest_fireball_owner_candidate=1`、`nearest_fireball_owner_confidence=100`、`nearest_fireball_owner_verified=1`、`nearest_fireball_owned_by_self_candidate=1`。
- 2026-06-08 fireball owner verified化後に `python scripts\nsmb_mvl_ai_render_playlog_svg.py logs\human-stage0-star-chase-005\client\ai-playlog.jsonl logs\codex-fireball-owner-verified-frame-4406.svg --player 1 --frame 4406` pass。旧ログでもSVG titleに `ownerCandidate=1 confidence=100 verified=1 source=slotKind kindName=player1` が出ることを確認。
- `logs\codex-visual-powerup-owner-fields-smoke-20260608`: `run-nsmb-mvl-human-recording.ps1 -NoPacketCapture -Frames 1200` の無操作client-only smokeで新規 `visualPowerupKindCandidate` / `visualPowerupSourceMask` / `canShootFireVisualCandidate` がJSONと監査に出ることを確認。無操作なので `recording-audit` は `nonzeroLabelRows=0` でfailし、これは期待通り。
- `logs\codex-visual-powerup-fields-scripted-20260608`: `tests\nsmb_us_direct_mvl_luigi_star_right.inputs` で2200Fを走らせ、後処理manifest/index/dataset/visual audit/recording auditまでpass。dataset 1332行、`self_visual_powerup_kind_candidate`、`self_visual_powerup_source_mask`、`self_can_shoot_fire_visual_candidate`、`nearest_fireball_owner_tracked` の列生成を確認。短いscripted smokeではfireballが出ないため、この時点ではowner tracking実戦品質は未確認だった。その後、fireball ownerはslot `+0x81` でverified化済み。
- 既存人間ログ `logs\nsmb-mvl-human-recording-stage0-20260608-012640` は新フィールド追加前のログとして後処理互換性を確認。Fire場面はある。旧ログに `kind` が残っているfireball slotは監査/dataset/SVG側でownerを補正できるが、`visualPowerupKindCandidate` は過去ログからは復元できないため、新ログで取り直す必要がある。
- 2026-06-08 `run-nsmb-mvl-human-recording.ps1` に `-Scenario` を追加後、PowerShell Parser確認pass。`-DryRun -NoPacketCapture -Frames 120 -Scenario fire -LogDir logs\codex-human-recording-scenario-dryrun-20260608` で `recording-session.json` の `scenario=fire` と、manifest作成postCommandの `--scenario "fire"` を確認。
- 2026-06-08 `run-nsmb-mvl-human-recording.ps1` の未指定match seed自動生成追加後、PowerShell Parser確認pass。`logs\codex-human-recording-autoseed-dryrun-a-20260608` と `logs\codex-human-recording-autoseed-dryrun-b-20260608` のdry-runで異なる `mvlMatchSeed` と `--match-seed` が入ること、`logs\codex-human-recording-fixedseed-dryrun-20260608` で `-MvlMatchSeed 0x00000001` が維持されることを確認。
- 2026-06-08 `logs\human-stage0-star-chase-001` から `005` まで5本の手動star-chaseログを後処理。全5本でrecording audit pass、playerFoundRatio 0.979-0.984、nonzeroLabelRows 3280-4623、starPickup 6-7、coinChange 14-19、itemVisible 699-1886、playerDeath 2-4を確認。`visualStateMissing=0/0`、`invincibilityUnknown=0/0`。003/004/005でvisual Fire候補、004/005でfireball slotを確認したが、004では横ラップ未補正のowner推定がP0へ寄る場面があったため、fireball owner推定とrelative dxに横ラップ補正を追加した。
- 2026-06-08 `logs\human-stage0-item-box-001` を後処理。`recording-audit.json` pass、dataset 6012行、`itemVisible=1562`、`blockCandidateVisible=1898`、`coinChange=33`、`visualPowerupChange=1`。Luigiのcoinが `7 -> 0` になる8コイン成立候補はframe 2012 / 3111 / 4582 / 6379で、直後に `objectId=0x01F` のitemが出る。例: frame 2013は `coin_reward_item_found=1`、`coin_reward_item_settings=0x00011089`、`coin_reward_item_powerup_kind_candidate=2`、frame 2118は同item取得近傍で `visualPowerupKindCandidate 1 -> 2`。frame 6380 / 6456の `coin_reward_item_settings=0x0001108B` は当時疑似マメ候補としていたが、後続の手動目視ログで甲羅と確認したため、現在は `shell_candidate` として扱う。raw settings/vtable/objectIdをCSVに残しているため、再生成で新mappingを反映できる。
- 2026-06-08 `logs\human-stage0-item-box-002` でも8コイン報酬item列のdataset生成を確認。dataset 4230行、報酬window内item settingsとして `0x00011099`、`0x00090000`、`0x00011088`、`0x00011089`、`0x000D0002` を確認し、このログでは疑似マメ候補avoid行は0件だった。
- 2026-06-08 `logs\codex-human-imitation-baseline-20260608`: 手動human 8本 (`star-chase` 5本、`item-box` 2本、旧手動1本) を `recordings-index.json` に束ね、`nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source player --require-player-found` でdataset 36281行を生成。`nsmb_mvl_ai_audit_recordings.py` は8本すべてpass。`nsmb_mvl_ai_train_imitation.py ... --epochs 800 --lr 0.05 --split-by-recording --validation-fraction 0.25 --seed 1` で `human-stage0-player1-imitation.npz` を学習し、train 25722行 / val 10559行、train button acc 0.962 exact 0.630、validation button acc 0.925 exact 0.421。validation holdoutは recording 0 (`human-stage0-star-chase-001`) と recording 5 (`human-stage0-item-box-001`)。全datasetへのoffline predictionは button acc 0.951 exact 0.569。threshold sweepではvalidation exactが0.40で0.431、0.50で0.421。`human-stage0-item-box-001-model.inputs` も生成し、6012予測を426 spanへ圧縮できることを確認したが、melonDS閉ループ実走評価は未実施。
- 2026-06-07 damage guard/shell state追加後に `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_create_recording_manifest.py scripts\nsmb_mvl_ai_render_playlog_svg.py scripts\nsmb_mvl_ai_audit_visual_state.py` pass。
- 2026-06-07 damage guard/shell state追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。
- `logs/codex-ai-visual-guard-smoke-20260607`: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-split-local-input-smoke.ps1 -Frames 950 -MvlStage 0 -AIPlayLogInterval 30 -AllowRemoteInputTimeoutFallback -InternalWaitTimeoutMs 1 -SkipGameStateComparison -SkipMovementProbe ...` pass。AI play logの `players[].damageCooldown`、`visualState.damageGuardTimer`、`damageGuardFlag`、`damagePhysicsGuard`、`shellState`、`invincibleKnown=1` を確認。
- 同ログから `python scripts\nsmb_mvl_ai_audit_visual_state.py <host-playlog> <client-playlog> --output logs\codex-ai-visual-guard-smoke-20260607\visual-state-audit.json` pass。`visualStateMissing=0/0`、`invincibilityUnknown=0/0`、可視未知objectなし、fireball slot 0件を確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py <host-playlog> logs\codex-ai-visual-guard-smoke-20260607\dataset.csv --player 1 --label-source applied` pass。CSVに `self_damage_guard_timer`、`self_damage_cooldown`、`self_shell_state`、`opponent_damage_guard_timer`、`opponent_shell_state` が出ることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_create_recording_manifest.py <host-playlog> logs\codex-ai-visual-guard-smoke-20260607\manifest.json --kind rule_ai --player 1 --label-source applied --stage 0 --frames 950` pass。manifestのplayer summaryへ damage guard/shell項目が入る。
- 同ログから `python scripts\nsmb_mvl_ai_render_playlog_svg.py <host-playlog> logs\codex-ai-visual-guard-smoke-20260607\frame-900.svg --player 1 --frame 900` pass。SVGヘッダに `invincible=0/guard=0/dmg=0` が出ることを確認。
- 2026-06-07 fireball raw slot監査追加後に `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_render_playlog_svg.py scripts\nsmb_mvl_ai_audit_visual_state.py` pass。
- 2026-06-07 fireball raw slot監査追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。
- `logs/codex-ai-fireball-raw-smoke-20260607`: raw slot監査追加後に `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-split-local-input-smoke.ps1 -Frames 950 -MvlStage 0 -AIPlayLogInterval 30 -AllowRemoteInputTimeoutFallback -InternalWaitTimeoutMs 1 -SkipGameStateComparison -SkipMovementProbe ...` pass。通常走行ではfireball slot 0件だが、JSON/dataset/SVG/auditの空slot経路が破綻しないことを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py <host-playlog> logs\codex-ai-fireball-raw-smoke-20260607\dataset.csv --player 1 --label-source applied` pass。CSVに `nearest_fireball_state_byte82`、`nearest_fireball_state_byte84`、`nearest_fireball_state_byte86`、`nearest_fireball_debug_word0` が出ることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_audit_visual_state.py <host-playlog> <client-playlog> --output logs\codex-ai-fireball-raw-smoke-20260607\visual-state-audit.json` pass。`visualStateMissing=0/0`、`invincibilityUnknown=0/0`、可視未知objectなし、fireball slot 0件を確認。
- 同ログから `python scripts\nsmb_mvl_ai_audit_visual_state.py <host-playlog> <client-playlog> --strict --output logs\codex-ai-fireball-raw-smoke-20260607\visual-state-audit-strict.json` pass。学習前strict gateが現在のstage 0短時間ログでは通ることを確認。
- 2026-06-08 片側物理入力ニュートラル化追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。`scripts\run-nsmb-mvl-human-recording.ps1 -DryRun -NoPacketCapture -Frames 120` でデフォルト `humanSide=client`、`neutralizeHostInput=true` を確認し、`-HumanSide host` では `neutralizeClientInput=true` になることを確認。
- 2026-06-08 human-recordingの内側script呼び出し修正後に、PowerShell Parser構文確認pass。`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-human-recording.ps1 -NoPacketCapture -Frames 120 -LogDir logs\codex-human-recording-parambind-fixed-20260608` で `Frames` 変換エラーが出ないこと、wrapper stderrが空であることを確認。
- 2026-06-08 手動人間記録の速度/同期実測。2窓で待ちを外したJITログは約59fpsまで出たが、peer接続しておらずhost/client同期記録としては不合格。同期待ちを戻すと `remote gameplay start ready wait timeout waitedMs=10000` や `peer wait timeout waitedMs=10000` で実プレイ不能。`run-nsmb-mvl-netplay-route-smoke.ps1` ではpeer接続自体は成立するため、2窓手動記録の同期経路は未解決として扱う。
- 2026-06-08 client-only権威記録の実測。`logs\codex-human-recording-single-client2-20260608` は1200Fを約57.6fpsで完走し、peer wait / remote gameplay start waitなし。無操作ログの後処理はdataset 332行まで生成し、録画監査は `nonzeroLabelRows=0` でfailすることを確認した。
- 2026-06-08 client-only権威記録の入力あり自動検証。`logs\codex-human-recording-single-client-scripted-20260608` は `tests\nsmb_us_direct_mvl_luigi_star_right.inputs` で2200Fを約55.5fps完走。後処理はmanifest/index/dataset/visual audit/recording auditまでpassし、dataset 1332行、`nonzeroLabelRows=1300`、`playerFoundRatio=0.9446` を確認した。
- 2026-06-08 人間手動ログ `logs\nsmb-mvl-human-recording-stage0-20260608-012640` を後処理。`recordingMode=single_client_authoritative`、dataset 4449行、`recording-audit.json` pass、`playerFoundRatio=0.9827`、`nonzeroLabelRows=3928`。eventは `starPickup=5`、`coinChange=17`、`blockCandidateVisible=1444`、`itemVisible=1473`、`fireballActive=508`。`visual-state-audit.json` は `visualStateMissing=0/0`、`invincibilityUnknown=0/0`、fireball slot 613件、ただし `fireballOwnerLowConfidence=541`。代表SVGとして `frame-1099-item-spawn.svg`、`frame-1202-item-visible.svg`、`frame-2096-fireball.svg`、`frame-2715-fireball-star.svg` を生成した。Fire状態については `powerupValues` が0固定、`inventoryPowerupValues` がplayer1=1固定で、`actorPowerupState/FormState` の変化を併用する必要がある。
- `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。
- 2026-06-07 visual state追加後に `python -m py_compile scripts/nsmb_mvl_ai_build_dataset.py scripts/nsmb_mvl_ai_create_recording_manifest.py scripts/nsmb_mvl_ai_render_playlog_svg.py scripts/nsmb_mvl_ai_audit_visual_state.py` pass。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-human-recording.ps1 -DryRun -NoPacketCapture -Frames 120 -LogDir logs\codex-ai-visual-state-dryrun-20260607` pass。postCommandsに `nsmb_mvl_ai_audit_visual_state.py` と `visual-state-audit.json` が入ることを確認。
- `scripts\run-nsmb-mvl-split-local-input-smoke.ps1` / `scripts\run-nsmb-mvl-human-recording.ps1` のPowerShell Parser構文確認pass。
- `logs/codex-ai-visual-state-smoke-20260607`: `cmake --build ... --target melonDS -j 4` pass後、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-split-local-input-smoke.ps1 -Frames 950 -MvlStage 0 -AIPlayLogInterval 30 -AllowRemoteInputTimeoutFallback -InternalWaitTimeoutMs 1 -SkipGameStateComparison -SkipMovementProbe ...` pass。短いsmokeなので強さ評価ではなく、stage 0 gameplay中のAI play log schema確認用。
- 旧 `logs/codex-ai-visual-state-smoke-20260607` では当時の未取得状態として `players[1].visualState.powerup.raw=0` / `inventoryPowerup.raw=1` / `hasReserveItemCandidate=1` / `invincibleKnown=0` がJSONLへ出ることを確認。`specialObjects.fireballs` はhandler/words/activeSlots/slotsを保持し、今回の通常走行ではslot 0件。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。CSVに `self_inventory_powerup`、`self_has_reserve_item_candidate`、`opponent_powerup`、`opponent_dead`、`nearest_fireball_owner_candidate` が出ることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_create_recording_manifest.py ... --kind rule_ai --player 1 --label-source auto --stage 0 --max-event-samples 5` pass。manifestのplayer summaryへ `powerupName` / `inventoryPowerupName` / invincibility候補が入る。
- 旧同ログから `python scripts\nsmb_mvl_ai_audit_visual_state.py <host-playlog> <client-playlog> --output logs\codex-ai-visual-state-smoke-20260607\visual-state-audit.json` pass。当時は `visualStateMissing=0/0`、可視未知objectなし、fireball slot 0件、`invincibilityUnknown=5/5` で、無敵状態未取得が監査上の課題として残ることを確認。その後 `logs/codex-ai-visual-guard-smoke-20260607` で `invincibilityUnknown=0/0` まで解消済み。
- 同ログから `python scripts\nsmb_mvl_ai_render_playlog_svg.py ... --player 1 --frame 900` pass。SVGヘッダに `visualState`、special fireball slotが出る経路を確認。今回の走行ではfireball slotは0件。
- 同ログの小datasetで `python scripts\nsmb_mvl_ai_train_imitation.py ... --epochs 20 --lr 0.05` pass。3行のみのpipeline smokeであり、AI性能評価ではない。
- `logs/codex-ai-playlog-label-smoke-20260607`: rule AI remote + PacketBridge JIT helper + `MELONDS_NSML_AI_PLAY_LOG` で1600F pass。`ai-playlog.jsonl` は27行、frame 810-1590、player actorあり25行、`appliedPlayer1` valid 24行。
- 同ログでカテゴリ `big_star_actor`、`camera`、`moving_hazard`、`player`、`stage_actor_manager`、`stage_controller`、`stage_scene` を確認。frame 900 では `appliedPlayer1.heldHex=0x810` が出ており、remote CPUの入力ラベルが保存されている。
- `python scripts\nsmb_mvl_ai_build_dataset.py logs\codex-ai-playlog-label-smoke-20260607\ai-playlog.jsonl logs\codex-ai-playlog-label-smoke-20260607\ai-dataset-player1.csv --player 1 --require-player-found` pass。24行のCSVが生成され、`label_held`、button別 `label_*`、self/opponent/target/camera/object/nearestカテゴリ特徴を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-playlog-label-smoke-20260607\ai-dataset-player1.csv logs\codex-ai-playlog-label-smoke-20260607\ai-imitation-player1.npz --epochs 200 --lr 0.05` pass。24行の小データで学習と `.npz` 保存が動作することを確認。これはパイプライン検証であり、強さ評価ではない。
- `logs/codex-ai-visual-wrapx-smoke-20260607`: visualSummary追加後のrule AI remote smoke 1600F pass。JSONL 27行、`visualSummary` 全行あり。frame 870で `visibleCamera0X=10`、`visibleCamera1X=11`、player objectの `screen.camera0.inViewX=1` を確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found` pass、24行CSV生成。`visible_camera*_x` と `count_*` 特徴が追加された状態で `python scripts\nsmb_mvl_ai_train_imitation.py ... --epochs 200 --lr 0.05` pass。
- `python scripts\nsmb_mvl_ai_inspect_playlog.py logs\codex-ai-visual-wrapx-smoke-20260607\ai-playlog.jsonl --player 1 --limit 8` pass。frame 900で入力 `LY`、player1座標、Big Star相対 `696,104`、moving hazard相対 `127,-8`、カテゴリ数を表で確認。
- `logs/codex-ai-object-catalog-smoke-20260607`: object catalog追加後のrule AI remote smoke 1600F pass。`stage_fx`、`mvl_object267`、`vs_connect` のカテゴリ化を確認。未知のまま残る代表IDは当時 `0x021`、`0x145`。
- `logs/codex-ai-contact-smoke-20260607`: contact追加後のrule AI remote smoke 1600F pass。`python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 10` pass。frame 990でcontact `G`、frame 1020以降で `G+WR` を確認し、接地/壁接触が人間可読の表に出ることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found` pass、24行CSV生成。`self_contact_*` / `opponent_contact_*` は各33列、`count_coin`、`count_hazard`、`count_enemy_goomba`、`count_platform`、`count_warp_entrance` の列追加を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-contact-smoke-20260607\ai-dataset-player1.csv logs\codex-ai-contact-smoke-20260607\ai-imitation-player1.npz --epochs 200 --lr 0.05` pass。24行の小データで学習と `.npz` 保存が動作することを再確認。
- `logs/codex-ai-screenfall-smoke-20260607`: player screen/fallRisk追加後のrule AI remote smoke 1600F pass。`python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 10` pass。`y/bot` 列で screenY と camera bottom distance を確認し、frame 990 以降で `vy-` / `vy+` と接地状態が表に出ることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found` pass、24行CSV生成。`self_screen0_x`、`self_screen0_y`、`self_camera_bottom_distance0`、`self_near_camera_bottom0`、`self_below_camera0`、`self_vel_y_positive/negative` の列追加を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-screenfall-smoke-20260607\ai-dataset-player1.csv logs\codex-ai-screenfall-smoke-20260607\ai-imitation-player1.npz --epochs 200 --lr 0.05` pass。24行の小データで学習と `.npz` 保存が動作することを再確認。
- `logs/codex-ai-svg-smoke-20260607`: `item_spawn_effect` 分類とSVGレンダラ追加後のrule AI remote smoke 1600F pass。catalogで `0x0F0 settings=0x0109002F` が `item_spawn_effect` になることを確認。
- `python scripts\nsmb_mvl_ai_render_playlog_svg.py logs\codex-ai-svg-smoke-20260607\ai-playlog.jsonl logs\codex-ai-svg-smoke-20260607\frame-1020-player1.svg --player 1 --frame 1020` pass。`frame-1020-player1.svg` を生成し、player中心の相対配置を目視確認できる成果物が作れることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found` pass、24行CSV生成。`python scripts\nsmb_mvl_ai_train_imitation.py ... --epochs 200 --lr 0.05` pass。
- `logs/codex-ai-vtable-smoke-20260607`: object `offset` / `vtable` 追加後のrule AI remote smoke 1600F pass。catalogで `sampleVTable` が出力され、unknown objectにもvtableが残ることを確認。`0x021` と `0x022` が同じ `0x021331E8` を持つことを確認。
- `logs/codex-ai-vtable-category-smoke-20260607`: `0x021` を `big_star_related` に分類後のrule AI remote smoke 1600F pass。catalogで `0x021 settings=0x00000000 big_star_related sampleVTable=0x021331E8` を確認。inspect表、SVG生成、dataset生成、最小imitation trainまでpass。
- 同ログで `python scripts\nsmb_mvl_ai_build_dataset.py ... --label-source auto|applied|player` を確認。`applied` は24行、`auto` と `player` は25行を生成。人間ログ相当の `playerN` ラベルでもCSV化できることを確認。`python -m py_compile` でAI関連Pythonスクリプト5本の構文確認pass。`auto` CSVから最小imitation train pass。
- `python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-vtable-category-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-vtable-category-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-vtable-category-smoke-20260607\ai-predictions-player1-auto.csv --limit 10` pass。10行サンプルで `button_acc=0.975`、`exact=0.800`、予測CSV生成を確認。
- `logs/codex-ai-collisionmgr-smoke-20260607`: CollisionMgr由来の足元地形候補追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。rule AI remote smoke 1600F pass。
- 同ログで `python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 10` pass。`terrain` 列に `D3`、`M2+D16`、sanity check外の `rawFFFFDA80` が表示され、疑わしいbottom tile値を誤ってtile flag展開しないことを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found --label-source auto` pass。25行CSV生成。`self_collision_mgr_bottom_tile_sane` は `0/1` 両方を含み、sanity check外のbottom tile category列は0へ落とすことを確認。
- `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_inspect_playlog.py scripts\nsmb_mvl_ai_predict_imitation.py` pass。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-collisionmgr-smoke-20260607\ai-dataset-player1-auto-sane.csv logs\codex-ai-collisionmgr-smoke-20260607\ai-imitation-player1-auto-sane.npz --epochs 200 --lr 0.05` pass。`python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-collisionmgr-smoke-20260607\ai-imitation-player1-auto-sane.npz logs\codex-ai-collisionmgr-smoke-20260607\ai-dataset-player1-auto-sane.csv logs\codex-ai-collisionmgr-smoke-20260607\ai-predictions-player1-auto-sane.csv --limit 10` pass。10行サンプルで `button_acc=0.975`、`exact=0.800`。
- 逆アセンブル確認: `CollisionMgr::updatePlayer` / `updatePlayerVertical` / `getBottomModifierType` / `getTopModifierType` / `getSideModifierType` / `Player::applyTileDamage` / `Player::updateCollision` を `tools\nsmb_us_rom_tool.py disasm` で確認。`CollisionMgr +0x7C` collision result、`+0x80` ground collision、`+0x90/+0x92` attached tile、`+0x98/+0x9C/+0xA0` bottom/top/side modifier tile type、Player `+0xBB2/+0xBB3` tile damage flags/typeをログsourceに採用した。
- `logs/codex-ai-tiledamage-smoke-20260607`: CollisionMgr offset補正と `tileDamage` 追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。rule AI remote smoke 1600F pass。
- 同ログで `python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 10` pass。`terrain` 列で `terrain0` / `C` が表示され、旧暫定offset由来の `rawFFFFDA80` は出なくなった。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found --label-source auto` pass。25行CSV生成。`self_collision_mgr_ground_collision`、`self_collision_mgr_bottom_modifier_tile_type`、`self_tile_damage_flags/type/active`、`self_bottom_modifier_tile_*` の列追加を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-tiledamage-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-tiledamage-smoke-20260607\ai-imitation-player1-auto.npz --epochs 200 --lr 0.05` pass。`python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-tiledamage-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-tiledamage-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-tiledamage-smoke-20260607\ai-predictions-player1-auto.csv --limit 10` pass。10行サンプルで `button_acc=0.958`、`exact=0.700`。
- `python scripts\nsmb_mvl_ai_render_playlog_svg.py logs\codex-ai-tiledamage-smoke-20260607\ai-playlog.jsonl logs\codex-ai-tiledamage-smoke-20260607\frame-1050-player1.svg --player 1 --frame 1050` pass。
- `logs/codex-ai-screen-inviewy-smoke-20260607`: `screen.camera*.inViewY` 追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。rule AI remote smoke 1600F pass。
- 同ログでJSON spot check pass。frame 870のplayer/object `screen.camera0` に `inViewX`、`inViewY`、`inView` が入り、player例は `inViewX=1` / `inViewY=0` / `inView=0`。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found --label-source auto` pass。25行CSV生成。`self_screen0_in_view_y`、`self_screen1_in_view_y`、`opponent_screen0_in_view_y`、`opponent_screen1_in_view_y` の列追加を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-screen-inviewy-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-screen-inviewy-smoke-20260607\ai-imitation-player1-auto.npz --epochs 200 --lr 0.05` pass。`python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-screen-inviewy-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-screen-inviewy-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-screen-inviewy-smoke-20260607\ai-predictions-player1-auto.csv --limit 10` pass。10行サンプルで `button_acc=0.975`、`exact=0.800`。
- `python scripts\nsmb_mvl_ai_render_playlog_svg.py logs\codex-ai-screen-inviewy-smoke-20260607\ai-playlog.jsonl logs\codex-ai-screen-inviewy-smoke-20260607\frame-1050-player1.svg --player 1 --frame 1050` pass。
- `logs/codex-ai-stage-layout-category-smoke-20260607`: `0x145` を `stage_layout` に分類後、rule AI remote smoke 1600F pass。catalogで `0x145 settings=0x00000000 stage_layout sampleVTable=0x02123D6C` を確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --require-player-found --label-source auto` pass。25行CSV生成。`count_stage_layout=1`、`count_object=0` を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-stage-layout-category-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-stage-layout-category-smoke-20260607\ai-imitation-player1-auto.npz --epochs 200 --lr 0.05` pass。`python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-stage-layout-category-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-stage-layout-category-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-stage-layout-category-smoke-20260607\ai-predictions-player1-auto.csv --limit 10` pass。10行サンプルで `button_acc=0.958`、`exact=0.700`。
- `python scripts\nsmb_mvl_ai_render_playlog_svg.py logs\codex-ai-stage-layout-category-smoke-20260607\ai-playlog.jsonl logs\codex-ai-stage-layout-category-smoke-20260607\frame-1050-player1.svg --player 1 --frame 1050` pass。
- 逆アセンブル確認: `StageLayout::getTileBehavior` / `getChunkID` / `readTileBehaviour` / `CollisionMgr::scanSolidTile` / `getTileType` を `tools\nsmb_us_rom_tool.py --rom roms\nsmb-us.nds disasm ... --overlay-id 0` で確認。`Stage::stageLayout=0x020CAD40`、chunk pointer table `0x020CAFE0`、base tile behavior table `0x020C8484`、dynamic tile behavior table pointer `0x020CAD28`、solid mask `0x08990000` をAI tile probe sourceに採用した。旧メモの `0x0208AF3C` は `readTileBehaviour` の `tileId >= 0x100` 分岐の参照先ではなかった。
- `logs/codex-ai-tileprobe-yfix-smoke-20260607`: StageLayout tile probe追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。host単体の標準split smoke 1150F pass。AI play logは10行、frame 900以降で `players[1].tileProbe.found=1`、`center/below/aheadBody/aheadBelow` などの `tileId=0x001`、`behavior=0x0000002A` が保存されることを確認。
- 同ログで `python scripts\nsmb_mvl_ai_inspect_playlog.py logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-playlog.jsonl --player 1 --limit 8` pass。`probe` 列に `hole:ab:001,ad:001,b:001` のように前方/下方向のtile idが表示されることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。9行CSV生成。`self_tile_probe_found`、`self_tile_probe_holeAhead`、`self_tile_probe_below_tile_id`、`self_tile_probe_aheadBelow_tile_id` などの列追加を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-imitation-player1-auto.npz` pass。`python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-predictions-player1-auto.csv` pass。9行の小データで `button_acc=1.000`、`exact=1.000`。これはパイプライン検証であり、強さ評価ではない。
- `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_inspect_playlog.py scripts\nsmb_mvl_ai_train_imitation.py scripts\nsmb_mvl_ai_predict_imitation.py` pass。
- `python -m py_compile scripts\nsmb_mvl_ai_render_playlog_svg.py` pass。
- `python scripts\nsmb_mvl_ai_render_playlog_svg.py logs\codex-ai-tileprobe-yfix-smoke-20260607\ai-playlog.jsonl logs\codex-ai-tileprobe-yfix-smoke-20260607\frame-1020-player1-tileprobe.svg --player 1 --frame 1020` pass。SVG内に `tileProbe center/feet/below/aheadBody/aheadFeet/aheadBelow/ahead2Feet/ahead2Below/above` の矩形とtile id/behavior titleが出ることを確認。
- `logs/codex-ai-ruleai-tileprobe-smoke-20260607`: RuleAI `FrameState` への tileProbe summary接続後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。`MELONDS_NSML_RULE_AI=1` / `MELONDS_NSML_RULE_AI_TRACE=1` つきのhost単体標準split smoke 1150F pass。起動時のRuleAI enabledログは確認したが、このharnessでは入力決定traceは出なかったため、実際のRuleAI操作経路でterrain jumpが発火するかは次回の対象。
- 同ログで `python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 8` pass、`python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。tileProbe列は引き続き生成され、既存tileProbeモデルで `python scripts\nsmb_mvl_ai_predict_imitation.py ... --limit 5` pass。
- `logs/codex-ai-tileprobe-lr-smoke-20260607`: 左右固定tileProbe追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。host単体標準split smoke 1150F pass。AI play logは10行、frame 1020で `wallLeft=0`、`holeLeft=1`、`wallRight=0`、`holeRight=1`、`leftBody/leftBelow/rightBody/rightBelow` samplesが保存されることを確認。
- 同ログで `python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 6` pass。`probe` 列に `hole+HL+HR:ab:001,ad:001,lb:001,rb:001,b:001` のように左右穴候補と左右tile idが表示されることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。9行CSV生成。`self_tile_probe_wallLeft`、`self_tile_probe_holeLeft`、`self_tile_probe_wallRight`、`self_tile_probe_holeRight`、`self_tile_probe_leftBelow_found`、`self_tile_probe_rightBelow_found` の列追加を確認。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-tileprobe-lr-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-tileprobe-lr-smoke-20260607\ai-imitation-player1-auto.npz` pass。`python scripts\nsmb_mvl_ai_predict_imitation.py logs\codex-ai-tileprobe-lr-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-tileprobe-lr-smoke-20260607\ai-dataset-player1-auto.csv logs\codex-ai-tileprobe-lr-smoke-20260607\ai-predictions-player1-auto.csv` pass。9行の小データで `button_acc=1.000`、`exact=1.000`。
- `logs/codex-ai-stage0-ruleai-groundfallback-block-smoke-20260607`: stage 0対象のRuleAI専用スモーク1700F pass。`cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass後に実施し、`NSMB RuleAI: inst=... terrain=...` traceが実操作経路で出ることを確認。frame 960以降で `terrain=ground:1 ahead:0/0 left:0/0 right:0/0` となり、接地contactフォールバックで偽hole jumpが抑えられることを確認。
- 同ログで `players[].tileProbe.samples[].block` がJSONLに出力され、`scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。28行CSV生成、`self_tile_probe_*_block_*` / `opponent_tile_probe_*_block_*` は合計374列。今回のseedでは実際のblock sampleは0件だったが、直前の `logs/codex-ai-stage0-ruleai-offset48-block-smoke-20260607` ではframe 1230の `leftBody` で `tileId=71`、`question=1`、`itemBox=1`、`storageContents=7` を確認。
- 同ログから `python scripts\nsmb_mvl_ai_train_imitation.py ... --epochs 200 --lr 0.05` pass。28行の小データで `train_button_acc=0.992`、`val_button_acc=0.833`。`python scripts\nsmb_mvl_ai_predict_imitation.py ... --limit 10` pass、`button_acc=0.975`、`exact=0.900`。`python scripts\nsmb_mvl_ai_render_playlog_svg.py ... --frame 1200` pass。
- `logs/codex-ai-stage0-effective-tileprobe-smoke-20260607`: effective tileProbe summaryとsample `status` / `lowType` 追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass、`python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_inspect_playlog.py scripts\nsmb_mvl_ai_render_playlog_svg.py` pass。RuleAI専用スモーク1700F pass。
- 同ログで `python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 14` pass。frame 960以降の地上接触中は `probe` 列が `ground+suppress` になり、raw `hole*` を `effectiveHole*=0` へ抑えたことを確認。JSON spot checkで `summary.contactGround=1`、`effectiveGroundBelowSolid=1`、`holeSuppressedByContact=1`、`effectiveHoleAhead/Left/Right=0`、sample `feet.lowType=42` を確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。28行CSV生成。`self_tile_probe_effectiveGroundBelowSolid`、`self_tile_probe_holeSuppressedByContact`、`self_tile_probe_effectiveHoleAhead`、`self_tile_probe_feet_status`、`self_tile_probe_feet_lowType`、`self_tile_probe_below_status`、`self_tile_probe_below_lowType` の列追加を確認。
- 同ログから `python scripts\nsmb_mvl_ai_train_imitation.py ... --epochs 200 --lr 0.05` pass。28行の小データで `train_button_acc=0.992`、`val_button_acc=0.958`。`python scripts\nsmb_mvl_ai_predict_imitation.py ... --limit 10` pass、`button_acc=0.975`、`exact=0.900`。`python scripts\nsmb_mvl_ai_render_playlog_svg.py ... --frame 1200` pass。
- `logs/codex-ai-stage0-tile-catalog-smoke-20260607`: tile catalogスクリプト追加と失敗時tileID/behaviorTable保存後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass、`python -m py_compile scripts\nsmb_mvl_ai_catalog_tiles.py scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_inspect_playlog.py scripts\nsmb_mvl_ai_render_playlog_svg.py` pass。RuleAI専用スモーク1300F pass。
- 同ログで `python scripts\nsmb_mvl_ai_catalog_tiles.py ... --player 1 --limit 25` pass。接地中の成功サンプルは `tileId=0x001 behavior=0x0000002A lowType=0x2A solid=0 contactGround=1 effectiveGround=1 suppressed=1` が中心。`below/aheadBelow/*Below` の未取得は `status=6` かつ `tileId=0x110-0x113`、`behaviorTable=0x00000000` として見えるようになった。
- `logs/codex-tile-dynamic-behavior-fix-1600-20260613`: dynamic tile behavior table pointer修正後、`cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。stage 0 / seed `0x2f52869f` / RuleAI / 1600F / software rendererでhost/clientとも実走pass。新ログの `tileProbe.grid` 集計ではhost/clientとも `status` 未解決cellが0になり、旧ログで `tileId=0x110-0x113 status=6` だった地面系tileはbehavior/solidishつきで取得できるようになった。
- 同ログのclient frame 1500をスクショと可視化で照合し、画面上の床・土管・ブロック・hazardが `tileProbe.grid` / object表示に対応することを確認した。GUI/SVG表示側は `relTileX/relTileY` でgridを描き、camera/stage_controller/stage_layoutなどの内部objectを非表示にし、world Yを表示向けに反転する。
- `logs/codex-tile-dynamic-behavior-fix-1600-20260613` のclient frame 870で、GUI上のスターが2個に見える原因を調査した。実スターは `objectId=0x022 category=big_star_actor` だけで、もう一方は `objectId=0x021 category=big_star_related`、全frameで座標 `(0,0,0)` の補助actorだった。GUI/SVGでは `big_star_related` を非表示にし、スター表示は `big_star_actor` 1件だけにした。
- stage 0の敵カテゴリを確認し、旧ログでは `objectId=0x053 settings=0x00000000` が `moving_hazard` として出ていたが、これは `kGoombaObjectID` と同じIDで、画面上のクリボー相当だった。AIログ分類では `0x053/0x054/0x055` を `enemy_goomba`、`0x05E/0x05F` を `enemy_koopa` として出し、GUIでは `G` / `K` / `H` を区別表示する。内部同期名の `movingHazard` は既存のworld-state補正名として残る。
- `logs\nsmb-mvl-human-recording-stage0-20260613-191418\client\ai-playlog.jsonl` のframe 1590で、旧 `dropped_star_item` (`objectId=0x01F settings=0x00090002`) がP1付近の箱から出たitemで、frame 1615にP1 coinが `1 -> 2` へ増えることを確認した。AIログ生成は `coin_item` へ変更し、旧ログでもdataset/SVG/GUIで `coin_item` へ正規化する。frame 2255の `big_star_candidate` (`objectId=0x010C settings=0x00001120`) は本物の `big_star_actor` と別に存在する内部markerなので `big_star_marker` とし、GUI/SVGでは非表示、dataset/closed-loop評価のBig Star距離は `big_star_actor` だけを見る。検証では `logs\codex-category-fix-check-20260613.csv` のframe 1590で `count_coin_item=1` / `count_dropped_star_item=0`、frame 2255で `count_big_star_marker=1` / `count_big_star_candidate=0` を確認した。
- `logs\nsmb-mvl-human-recording-stage0-20260613-203615\client\ai-playlog.jsonl` のframe 1348で、落としたスターが `objectId=0x022 settings=0x00001112 vtable=0x021331E8` として出ることを確認した。`0x022 settings=0x00000001` の通常Big Starとは分け、settings高bitを除いた `0x1002/0x1012/0x1102/0x1112` を `dropped_star_item` に正規化する。datasetでは `count_dropped_star_item`、`nearest_dropped_star_item_*`、`nearest_item_is_dropped_star_candidate` に反映する。
- 同ログを `scripts\nsmb_mvl_ai_audit_items.py` で監査し、3クラスタを確認した。`item|0x001F|0x00090000|0x02124370` は後続検証で現在状態依存の `adaptive_powerup` と判明、`coin_item|0x001F|0x00090002|0x02124370` は `coin`、`dropped_star_item|0x0022|0x00001112|0x021331E8` は `dropped_battle_star` として出る。`logs\codex-item-audit-check-20260613\svgs` に各クラスタ代表SVGを生成し、`item-audit-clusters.csv` で件数と近傍状態変化を確認した。`run-nsmb-mvl-item-audit-sweep.ps1 -DryRun` もpassし、seed/policy/Battle Star差ごとの実験計画をJSON化できる。
- 2026-06-13 主要人間ログ11本を `logs\codex-item-kind-check-20260613\human-logs-item-audit.csv` に再監査し、item分類をdataset/C++ runtimeへ反映した。観測クラスタは13個で、未分類 `unknown` は0。確定は `coin` (`0x001F/0x00090002`)、`dropped_battle_star` (`0x0022/0x1002/0x1012/0x1102/0x1112` normalized)、`fire_flower` (`0x001F/0x00011089`)。当時 `0x00090000` は `fire_flower_candidate` としていたが、後続ログで `adaptive_powerup` に修正した。候補は `super_mushroom_candidate` (`0x00011088`, confidence 1)、`invincible_star_candidate` (`0x00011081`, confidence 1)、`unknown_item_variant_candidate` (`0x000D0000/0x000D0002`, confidence 1)。後続ログで `0x0001108B` をshell、`0x00011081` をinvincible starに修正した。
- 2026-06-13 `logs\nsmb-mvl-human-recording-stage0-20260613-232727\client\ai-playlog.jsonl` のframe 32280で、ユーザー目視により `objectId=0x001F settings=0x0001108B vtable=0x02124370` のitemが甲羅であることを確認。`logs\codex-shell-item-check-20260613\window-32000-34000.jsonl` を切り出して監査し、同trackが32112から出現して32280に残っていることを確認した。dataset/C++ runtime/auditでは `0x0001108B` を `shell` / `kind_confidence=3` / `powerup_kind_candidate=4` とし、過去の `mini_mushroom_candidate` / `avoid_candidate` 扱いを取り消した。
- 2026-06-13 `logs\nsmb-mvl-human-recording-stage0-20260613-234501\client\ai-playlog.jsonl` のframe 6875で、ユーザー目視により `objectId=0x001F settings=0x00011099 vtable=0x02124370` のitemがマメで、frame 7000ではLuigiがマメ状態になっていることを確認。`logs\codex-mini-item-check-20260613\window-6800-7050.jsonl` を切り出して確認し、同trackはframe 6950付近まで残り、その後消える。dataset/C++ runtime/auditでは `0x00011099` を `mini_mushroom` / `kind_confidence=3` / `powerup_kind_candidate=3` / `avoid_candidate=1` とし、過去の `mega_mushroom_candidate` 扱いを取り消した。プレイヤー状態側も、actor powerup state/form state 4をマメとして扱うように修正し、既存ログdataset化時もframe 7000で `self_visual_powerup_kind_candidate=3` / `self_is_mini_visual_candidate=1` / `self_is_mini_candidate=1` になる。SVGも同じ補正で `visualPwr=3 miniVisual` を表示する。
- 2026-06-14 `logs\nsmb-mvl-human-recording-stage0-20260613-235503\client\ai-playlog.jsonl` で通常/スーパー/ファイアを照合した。開始後frame 900はLuigiが通常状態で、reserve inventory rawは1でも `self_visual_powerup_kind_candidate=0` とする。frame 1120の `objectId=0x001F settings=0x00090000` は通常状態ではスーパーitem、frame 1240はLuigiがスーパー状態 (`actorPowerupState/FormState=1`, visual kind 1)。同じ `0x00090000` はframe 1780ではスーパー状態に対するファイアitemで、frame 1950はFire状態 (`actorPowerupState/FormState=2`, visual kind 2)。dataset/C++ runtimeでは `0x00090000` を `adaptive_powerup` とし、selfの現在visual kindが0なら `kind_candidate=1`、1/2なら `kind_candidate=2` に文脈分類する。SVGもreserveを現在状態に混ぜず、1240は `visualPwr=1`、1950は `visualPwr=2 fireVisual` と表示する。
- 2026-06-14 `logs\nsmb-mvl-human-recording-stage0-20260614-000043\client\ai-playlog.jsonl` で無敵スターを照合した。ユーザー目視によりframe 22262はLuigiがスター無敵状態、frame 22534の `objectId=0x001F settings=0x00011081 vtable=0x02124370` は無敵スターitem。`logs\codex-star-item-check-20260614\window-22100-22620.jsonl` を切り出してdataset化し、frame 22262は `self_visual_powerup_kind_candidate=0` / `self_invincible_candidate=1` / `self_star_invincible_candidate=1`、frame 22534は `nearest_item_kind_candidate=6` / `nearest_item_kind_confidence=3` / `nearest_item_is_invincible_star_candidate=1` を確認した。SVG `frame-22262-star-state-updated.svg` は `inv=star_invincible_candidate starInv invincible=1` を表示する。visual auditは同windowで `starInvincibilityCandidateFrames=[0,521]`、`visualFireCandidateFrames=[0,0]`。
- 2026-06-14 `logs\nsmb-mvl-human-recording-stage0-20260614-010450\client\ai-playlog.jsonl` で巨大キノコを照合した。ユーザー目視によりframe 6789でLuigiの真上から落ちている `objectId=0x001F settings=0x00011085 vtable=0x02124370` が巨大キノコitem、frame 6977はLuigiが巨大状態。frame 6977では `actorPowerupState/FormState=3` なので、dataset/C++ runtime/SVG/visual auditではactor state/form 3を `visual_powerup_kind_candidate=5` / `is_mega_visual_candidate=1` として扱う。item側は `kind_candidate=5` / `kind_confidence=3` / `powerup_kind_candidate=5`。
- 2026-06-14 `logs\nsmb-mvl-human-recording-stage0-20260614-012339\client\ai-playlog.jsonl` で甲羅itemと甲羅状態を再照合した。frame 3059の `objectId=0x001F settings=0x0001108B vtable=0x02124370` は甲羅item、frame 3230はLuigiが甲羅状態。frame 3230では `shellState=2` かつ `actorPowerupState/FormState=5` なので、dataset/C++ runtime/SVG/visual auditではactor state/form 5も `visual_powerup_kind_candidate=4` / `is_shell_candidate=1` の根拠として扱う。切り出し `logs\codex-shell-item-check-20260614\window-3000-3260.jsonl` で、frame 3059は `nearest_item_kind_candidate=4` / `kind_confidence=3`、frame 3230は `self_visual_powerup_kind_candidate=4` / `self_shell_state=2` を確認した。
- 同ログから `python scripts\nsmb_mvl_ai_inspect_playlog.py ... --player 1 --limit 10` pass、`python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。15行CSV生成。`python scripts\nsmb_mvl_ai_train_imitation.py ... --epochs 200 --lr 0.05` pass、`python scripts\nsmb_mvl_ai_predict_imitation.py ... --limit 10` pass、`button_acc=0.975`、`exact=0.900`。`python scripts\nsmb_mvl_ai_render_playlog_svg.py ... --frame 1080` pass。
- `python -m py_compile scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_train_imitation.py scripts\nsmb_mvl_ai_create_recording_manifest.py scripts\nsmb_mvl_ai_make_recordings_index.py scripts\nsmb_mvl_ai_verify_replay.py scripts\nsmb_mvl_ai_export_viewer_data.py` pass。
- `logs/codex-ai-stage0-tile-catalog-smoke-20260607` の既存JSONLから `recording.json` 作成、`viewer-data.json` export、`recording.json` と同一JSONLの `nsmb_mvl_ai_verify_replay.py` pass。最終frame 1290一致。
- 同ログの `recording.json` / `recordings-index.json` から `nsmb_mvl_ai_build_dataset.py` pass。manifest/index経由で15行CSV生成。
- `python scripts\nsmb_mvl_ai_train_imitation.py logs\codex-ai-stage0-tile-catalog-smoke-20260607\ai-dataset-from-index-player1.csv logs\codex-ai-stage0-tile-catalog-smoke-20260607\ai-imitation-from-index-player1.npz --epochs 50 --lr 0.05 --split-by-recording` pass。1記録なので通常splitへフォールバックし、`train=12 val=3`。
- `scripts\run-nsmb-mvl-recording-replay.ps1` / `scripts\run-nsmb-mvl-split-local-input-smoke.ps1` / `scripts\run-nsmb-mvl-human-recording.ps1` のPowerShell Parser構文確認pass。
- `python -m py_compile scripts\nsmb_mvl_ai_create_recording_manifest.py scripts\nsmb_mvl_ai_verify_replay.py` pass。
- `python scripts\nsmb_mvl_ai_verify_replay.py logs\codex-ai-stage0-tile-catalog-smoke-20260607\recording.json logs\codex-ai-stage0-tile-catalog-smoke-20260607\ai-playlog.jsonl --checkpoint-interval 30 --checkpoint-start-frame 900 --max-checkpoints 3` pass。最終frame 1290と途中checkpoint 3件を同一ログで検証。
- `python scripts\nsmb_mvl_ai_create_recording_manifest.py ... --host-input-script ... --client-input-script ... --frames 1290` で、input script replay情報入りのテストmanifestを生成できることを確認。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-recording-replay.ps1 -RecordingManifest logs\codex-ai-stage0-tile-catalog-smoke-20260607\recording-replay-inputscript.json -DryRun -LogDir logs\codex-ai-recording-replay-dryrun-20260607` pass。manifestからhost/client input script、ROM、AI play log出力先、検証対象playlogを解決できることを確認。これは既存RuleAIログへ手動入力scriptを後付けしたdry-runであり、完全再現の実走検証ではない。
- `python scripts\nsmb_mvl_ai_create_recording_manifest.py logs\codex-ai-stage0-tile-catalog-smoke-20260607\ai-playlog.jsonl ... --max-event-samples 5` pass。`summary.eventSamples` に `playerDeath` frame 1200 と `blockCandidateVisible` frame 1230 / `leftBody` / `tileId=71` / `storageContents=7` が出ることを確認。
- `scripts\run-nsmb-mvl-manual-local.ps1` / `scripts\run-nsmb-mvl-human-recording.ps1` / `scripts\run-nsmb-mvl-split-local-input-smoke.ps1` / `scripts\run-nsmb-mvl-recording-replay.ps1` のPowerShell Parser構文確認pass。
- `python -m py_compile scripts\nsmb_mvl_ai_create_recording_manifest.py` pass。
- tempの `packet-replay.csv` とpacket replay用 `recording.json` を作り、`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-recording-replay.ps1 -RecordingManifest <temp-recording.json> -DryRun -LogDir logs\codex-ai-packet-replay-dryrun-20260607` pass。`replayMode=packet_replay`、host/client packet replay file、split起動引数を解決できることを確認。これはメタデータ/dry-run確認であり、melonDS実走の完全再現検証ではない。
- `python scripts\nsmb_mvl_ai_verify_replay.py logs\codex-ai-stage0-tile-catalog-smoke-20260607\recording.json logs\codex-ai-stage0-tile-catalog-smoke-20260607\ai-playlog.jsonl --scan-frames --mismatch-report-json ... --mismatch-report-csv ...` pass。同一ログの全frame scanで不一致なし。
- tempの改変AI play logで `python scripts\nsmb_mvl_ai_verify_replay.py <expected> <actual-mismatch> --scan-frames --mismatch-report-json <temp-diff.json> --mismatch-report-csv <temp-diff.csv>` を実行し、`first mismatch frame=900 ... hash` を検出。CSVには `frame,field,kind,expected,actual,message` が出ることを確認。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-recording-replay.ps1 -RecordingManifest logs\codex-ai-stage0-tile-catalog-smoke-20260607\recording-replay-inputscript.json -DryRun -ScanFrames -LogDir logs\codex-ai-replay-scan-dryrun-20260607` pass。replay起動計画に `scanFrames=true` と `replay-mismatch.json/csv` 出力先が入ることを確認。
- `scripts\run-nsmb-mvl-recording-postcommands.ps1` のPowerShell Parser構文確認pass。一時 `recording-session.json` で `-DryRun` pass、postCommandsの順序と件数を解決できることを確認。
- `logs/codex-ai-specialobjects-smoke-20260607`: `specialObjects` 追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。RuleAI smoke 1300F pass。17行JSONLすべてに `specialObjects` があり、今回の通常走行では `fireballs.active=0`、handler wordsは `fireballs.words[0]=0x021C29A0`、`projectiles.words[0]=0x021C6D0C` として取得できることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。CSVに `fireballs_active`、`fireballs_handler_word0`、`projectiles_handler_word0` が出ることを確認。
- 同ログから `python scripts\nsmb_mvl_ai_export_viewer_data.py ...` pass。viewer JSONに `specialObjects` が残ることを確認。`python scripts\nsmb_mvl_ai_create_recording_manifest.py ... --max-event-samples 5` pass、`summary.specialObjectFrames.fireballActive=0` と `eventSamples.fireballActive=[]` を確認。
- 逆アセンブル確認: `FireballHandler::spawn` / `FireballHandler::update` / `Fireball::create` / `Fireball::playerState` を `tools\nsmb_us_rom_tool.py disasm` と `tools\nsmb-mvl-rom\resources\symbols9.x` で確認。`Fireballs::activeFireballs=0x02129480`、`Fireballs::fireballHandler=0x02129484`、`Projectiles::projectileHandler=0x0212A680`、Fireball handler slotはhandler pointer + 4起点、16 slot、stride `0x8C`、active byte `+0x80`、pos `+0x10`、prev pos `+0x20`、velocity `+0x30` と判断した。
- `logs/codex-ai-fireball-slots-smoke-20260607`: Fireball slot logging追加後に `cmake --build build\release-windows-x86_64 --config Release --target melonDS -j 4` pass。RuleAI smoke 1300F pass。17行JSONLすべてに `specialObjects.fireballs.activeSlots` / `handlerPtr` / `slots` があり、通常走行では `active=0`、`activeSlots=0`、slot配列長0で欠落なし。
- 同ログから `python scripts\nsmb_mvl_ai_build_dataset.py ... --player 1 --label-source auto --require-player-found` pass。CSVに `fireballs_active_slots`、`fireballs_slot_count`、`nearest_fireball_*` が出ることを確認。`python scripts\nsmb_mvl_ai_create_recording_manifest.py ... --max-event-samples 5` pass、`summary.specialObjectFrames.fireballActive=0` を確認。`python scripts\nsmb_mvl_ai_export_viewer_data.py ... --max-frames 50` pass、viewer JSONにも `activeSlots` / `slots` が残ることを確認。
- `tools/nsmb-mvl-gui`: `pnpm run typecheck` pass。`pnpm biome check src/launcher/AIReplayViewer.tsx` pass。`pnpm vitest --config vitest.browser.config.ts run src/launcher/AIReplayViewer.browser.test.tsx` pass（1 file / 2 tests）。`pnpm run ci` は従来通り変更外ファイルを含むCRLF整形差分でBiome停止。
- `python -m py_compile scripts\nsmb_mvl_ai_audit_recordings.py scripts\nsmb_mvl_ai_create_recording_manifest.py scripts\nsmb_mvl_ai_build_dataset.py` pass。
- `python scripts\nsmb_mvl_ai_audit_recordings.py logs\codex-ai-specialobjects-smoke-20260607\recording.json --stage 0 --min-rows 1 --min-gameplay-rows 1 --min-player-found-ratio 0.5 --min-label-ratio 0.5 --min-nonzero-label-rows 1 --output ...` pass。statusは `pass`、警告としてquality unreviewedとplayer missing rowsを出す。
- 同じrecordingに `--require-event fireballActive:1` を指定すると、`event fireballActive count 0 < 1` でfailすることを確認。低頻度scenarioの取り忘れ検出に使える。
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\run-nsmb-mvl-human-recording.ps1 -LogDir logs\codex-ai-human-recording-dryrun-20260607 -Frames 120 -DryRun` pass。`recording-session.json` のpostCommandsにpacket変換、host/client manifest、index、dataset、`nsmb_mvl_ai_audit_recordings.py --require-packet-replay` が入ることを確認。
- `tools/nsmb-mvl-gui`: `pnpm run typecheck` pass。`pnpm biome check src/launcher/AIReplayViewer.tsx src/launcher/AIReplayViewer.browser.test.tsx` pass。`pnpm vitest --config vitest.browser.config.ts run src/launcher/AIReplayViewer.browser.test.tsx` pass（recording manifestのevent sample表示テストを含む）。
- `tools/nsmb-mvl-gui`: `pnpm vitest --config vitest.config.ts run` pass（4 files / 14 tests）。`pnpm vitest --config vitest.browser.config.ts run` pass（4 files / 13 tests）。`pnpm playwright test` pass（3 tests）。
- `tools/nsmb-mvl-gui`: `pnpm run ci` は `tsc --noEmit` 後の `biome check .` で停止。原因は変更外の既存ファイルを含むCRLF整形差分。変更ファイル単位のBiomeと全テストはpass。
- `python -m py_compile scripts\nsmb_mvl_ai_predict_input_script.py scripts\nsmb_mvl_ai_build_dataset.py scripts\nsmb_mvl_ai_predict_imitation.py` pass。
- `python scripts\nsmb_mvl_ai_predict_input_script.py logs\codex-ai-stage0-effective-tileprobe-smoke-20260607\ai-imitation-player1-auto.npz logs\codex-ai-stage0-effective-tileprobe-smoke-20260607\ai-playlog.jsonl logs\codex-ai-stage0-effective-tileprobe-smoke-20260607\ai-imitation-player1.inputs --player 1 --label-source auto --require-player-found --prefix-script tests\nsmb_us_direct_mvl_minimal_bootstrap.inputs --end-frame 1700 --predictions-csv ...` pass。28行から19 spanのinput scriptを生成し、bootstrap 0-839 と model予測 870-1700 が連結されることを確認。
- 同scriptで `--reaction-delay-frames 4 --mistake-rate 0.2 --mistake-mode drop-buttons --seed 7 --max-gap-fill 60` もpass。span開始が4frame遅れ、一部buttonがdropされる強さ調整用input scriptを生成できることを確認。
- 生成scriptをclient側入力として `run-nsmb-mvl-split-local-input-smoke.ps1` に渡したところ、melonDS側loaderはhost 5 span / client 24 spanを正常に読み込んだ。実走はframe 870のremote input timeoutでframe limit未到達。これはinput script parse失敗ではなく、split netplay harness側のpeer待ち/接続条件として別途詰める。
- default split local input smokeも同じくframe 870でremote input timeoutになり、モデルscript固有ではなくsplit harness/peer接続側の問題であることを確認。
- `scripts\run-nsmb-mvl-split-local-input-smoke.ps1 -AllowRemoteInputTimeoutFallback -InternalWaitTimeoutMs 1` でモデルinput scriptを900Fまで実行。host/clientともframe limitには到達し、wrapper上はchild smoke pass。ただしfallback neutralのためhost/clientの `vsStarActorX` がframe 900で不一致になり、同期検証はfail。速度はhost `fps=9.88`、client `fps=10.99` で、学習/大量評価用には不十分。
- 2026-06-14 grid-derived terrain summary変更後に `cmake --build build\release-windows-x86_64 --target melonDS --config Release` pass。`logs/codex-terrain-grid-derived-check-20260614` でv1→v2変換、legacy CSV生成、compact dataset生成pass。compact scalarは35列のまま、末尾7列は `tile_groundBelowSolid`、`tile_blockedAhead/Left/Right`、`tile_effectiveHoleAhead/Left/Right` で、`wall` 名は含まれない。frame 1200/1500では旧probeが足元床を見失って `probeGroundBelowSolid=0` でも、grid由来 `groundBelowSolid=1`、`blockedLeft/right=0` になり、階段床を左右壁として拾う問題が解消した。
- 同変更後のcompact小モデルで `python scripts\nsmb_mvl_ai_train_compact_imitation.py ... --epochs 8` pass、runtime export / fixture生成 pass、`nsmb_mvl_compact_cpp_predict` は64行で `python_exact=1`。`logs/codex-ruleai-terrain-grid-20260614` の3 seed x host/client RuleAI閉ループはpassし、aggregateは `avgDeathTransitions=0.6666666667`、`avgAliveRatio=0.9097605893`。前回の一部seedより死亡平均は少し悪いが、terrain summaryの正本化としては破綻なし。

## Next Actions

- 最優先: RuleAIをMario/Luigiで同じ判断ロジックにしたまま、スクショ/SVG/監査JSONで反復改善する。ただし目的は強いRuleAIそのものではなく、状態取得の妥当性確認と、RuleAI相手の人間ログ収集に耐える相手を作ること。穴中Big Starは「壁キックなしでは復帰困難なので取らない」方針に変更済みで、直近の3 seed x host/client 3600F評価ではMario/Luigiとも死亡平均 `0.0`。次は安全な床に支えられたBig Starだけを追う圧力、Goomba/Koopaを避けつつ踏めるタイミング、Fire開始時のfireball回避を、必ずPNG/SVGとログを突き合わせて詰める。P0/P1片側だけの補正、単純なhazard方向ジャンプ、全hazard範囲拡大、敵早期検出、空中blocked/guardBrakeの単純抑制は不採用済み。
- Fire Mario相手のログ収集導線を固める。`-ForcePlayerPowerups -ForcePlayerPowerup0 2 -ForcePlayerPowerup1 2` でFire開始、`fireball-audit.json` でowner verified、スクショでFire表示を確認できるようになった。次はMario側RuleAIまたはLuigi側RuleAIのどちらをFire相手にするかをscenarioごとに固定し、人間が「fireballを避ける」「踏みに来る相手を避ける」ログを取れる起動コマンドへ落とす。
- 次に、評価結果で弱いscenarioを特定してから、固定Mario/無操作Luigiログを増やすだけでなくscenario別に短い高品質人間ログを集める。まずは `star-chase`、`item-box`、`fire`、`enemy-hazard`、`recovery/fall-avoid`、`free-play` を各5-10本、各1-3分程度にし、`recording-audit`、`visual-state-audit`、`fireball-audit`、SVG/viewer/スクショ目視で破棄/採用を決める。Marioが動かないログは基礎操作データとして有効だが、対戦反応は学べないので単独で増やし続けない。
- RuleAIは「強いAI」を目指すより先に、相手・環境揺らし・DAgger用の分布拡張器として進化させる。Big Star追跡、落下回避、近傍item/box、単純攻撃/回避を入れ、人間がRuleAI相手に操作したログと、模倣AIがRuleAI相手に失敗したログを集める。
- 模倣学習は6物理ボタン + factor化action labelへ移行済みなので、次は短い人間ログをscenario別に増やし、前回入力/hold duration/reaction delay、失敗frameのDAgger用抽出、短いノイズ入力の抑制を入れる。Fireは `run` と `fire=press` を分けたまま、出力時にY release/pressでedgeを作る方式を継続検証する。
- DAgger風の反復を入れる。人間ログで初期モデルを作る、モデルを閉ループで走らせる、失敗/分布外/迷いframeをviewerで見つける、人間が同じ場面や近いscenarioをプレイして追加ラベルを入れる、datasetへ集約して再学習する。この反復を自己対戦の前に数回回す。
- 自己対戦は、閉ループ評価で最低限「落ちずに動く」「星へ近づく」「item/fireballを扱う」が確認できてから始める。初期相手は無操作Mario、次にRuleAI、次に過去版モデル、最後にcurrent vs currentへ進める。報酬は勝敗だけでなく、star差、死亡、Big Star接近、item取得、攻撃/回避、時間生存を混ぜる。
- 学習/自己対戦用には、2窓GUI smokeではなく高速runnerを用意する。目標は評価で60fps安定、自己対戦データ生成で数百fps以上。描画/音声/stdout/log flushを切り、必要な観測と入力だけを残す。
- 状態取得の残課題は並行して潰す。StageLayout tile probeの低位tile意味、legacy `storageContents/itemBox` と実アイテム中身の対応、見えているblockとは別レイヤーの隠し/救済item boxが存在するか、block/item boxの使用後tile変化、8コイン報酬item settings、スター/ダメージ/powerup無敵の原因分類、fireball寿命/当たり判定state、落下死ライン/ステージ境界を、実ログとSVG/viewerで照合する。基本の敵避け、スター追跡、穴回避が崩れない範囲まで確認できたら、Fire Mario開始またはFire item取得済みログでfireball owner/source/state/facing/lifetimeの画面照合を進める。
