#pragma once

// AI observation/logging code is still compiled as part of NsmbNetplayPoC.cpp.
// The implementation depends on internal sample/cache types declared in that
// translation unit, so NsmbAiObservation.cpp is intentionally included after
// those dependencies are available.
